---
title: "MISTY: High-Throughput Motion Planning via Mixer-based Single-step Drifting"
title_zh: MISTY：通过基于 Mixer 的单步漂移实现高吞吐量运动规划
authors: "Yining Xing, Zehong Ke, Yiqian Tu, Zhiyuan Liu, Wenhao Yu, Jianqiang Wang"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21489v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 9.0
evidence: 用于自动驾驶的基于扩散的生成式运动规划器
tldr: 针对自动驾驶中扩散模型规划器推理延迟高的问题，本文提出MISTY，一种基于单步推理的高吞吐量生成式运动规划框架。通过结合向量化子图编码器、VAE潜空间流形和轻量化MLP-Mixer解码器，并引入潜空间漂移损失，MISTY在保持极高推理速度的同时，实现了超越专家演示的主动避障和超车能力，在nuPlan基准测试中达到SOTA性能。
selection_source: fresh_fetch
motivation: 现有的基于扩散模型的运动规划器因迭代推理导致延迟过高，难以满足自动驾驶的实时性需求。
method: 采用VAE构建潜空间，利用MLP-Mixer进行单步推理，并通过潜空间漂移损失在训练阶段模拟复杂的分布演化。
result: 在nuPlan Test14-hard测试中取得SOTA成绩，推理速度超过99 FPS，端到端延迟仅为10.1 ms。
conclusion: MISTY证明了通过单步生成模型结合潜空间优化，可以在显著提升规划效率的同时，生成比专家数据更具主动性的安全轨迹。
---

## 摘要
多模态轨迹生成对于安全自动驾驶至关重要，然而现有的基于扩散（diffusion-based）的规划器由于迭代神经函数评估而面临高推理延迟。本文提出了 MISTY（基于 Mixer 推理的单步轨迹漂移产出），这是一种高吞吐量的生成式运动规划器，通过纯单步推理实现了最先进的闭环性能。MISTY 集成了一个向量化子图（Sub-Graph）编码器以捕获环境上下文，一个变分自编码器（VAE）将专家轨迹结构化为紧凑的 32 维潜流形（latent manifold），以及一个超轻量级的 MLP-Mixer 解码器以消除二次方复杂度的注意力机制。重要的是，我们引入了一种潜空间漂移损失（latent-space drifting loss），将复杂的分布演化完全转移到训练阶段。通过制定显式的引力和斥力，该机制使模型能够合成新颖的主动决策，例如在原始专家演示中几乎不存在的主动超车。在 nuPlan 基准测试上的广泛评估表明，MISTY 在具有挑战性的 Test14-hard 划分上取得了最先进的结果，在非反应式和反应式设置下的综合得分分别为 80.32 和 82.21。MISTY 的运行速度超过 99 FPS，端到端延迟为 10.1 ms，与迭代扩散规划器相比，在实现显著鲁棒生成的同时，提供了数量级的加速。

## Abstract
Multi-modal trajectory generation is essential for safe autonomous driving, yet existing diffusion-based planners suffer from high inference latency due to iterative neural function evaluations. This paper presents MISTY (Mixer-based Inference for Single-step Trajectory-drifting Yield), a high-throughput generative motion planner that achieves state-of-the-art closed-loop performance with pure single-step inference. MISTY integrates a vectorized Sub-Graph encoder to capture environment context, a Variational Autoencoder to structure expert trajectories into a compact 32-dimensional latent manifold, and an ultra-lightweight MLP-Mixer decoder to eliminate quadratic attention complexity. Importantly, we introduce a latent-space drifting loss that shifts the complex distribution evolution entirely to the training phase. By formulating explicit attractive and repulsive forces, this mechanism empowers the model to synthesize novel, proactive maneuvers, such as active overtaking, that are virtually absent from the raw expert demonstrations. Extensive evaluations on the nuPlan benchmark demonstrate that MISTY achieves state-of-the-art results on the challenging Test14-hard split, with comprehensive scores of 80.32 and 82.21 in non-reactive and reactive settings, respectively. Operating at over 99 FPS with an end-to-end latency of 10.1 ms, MISTY offers an order-of-magnitude speedup over iterative diffusion planners while while achieving significantly robust generation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MISTY** 的新型自动驾驶运动规划框架，旨在解决生成式模型在实时应用中的推理延迟瓶颈。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于扩散模型（Diffusion-based）的规划器虽然能生成高质量的多模态轨迹，但由于需要多次迭代推理（NFE），导致计算延迟高，难以满足自动驾驶系统的实时性要求。
*   **研究动机**：
    *   传统的回归模型（如 MSE 损失）容易导致“众数塌陷”，生成均值化的不合理轨迹。
    *   现有的单步生成方法（如一致性模型）在复杂场景下容易出现“特征漂移”，导致轨迹偏离可行驶区域或发生碰撞。
    *   需要在保证多模态生成质量的同时，实现极高的推理吞吐量。

### 2. 方法论
MISTY 的核心思想是将复杂的分布演化过程从推理阶段转移到训练阶段，通过单步前向传播完成高质量规划。
*   **向量化子图编码器（Sub-Graph Encoder）**：采用类似 VectorNet 的架构，将高精地图和智能体历史轨迹转化为向量表示，利用可学习的注意力池化（Attention Pooling）捕获环境上下文。
*   **VAE 潜空间流形**：使用变分自编码器（VAE）将高维专家轨迹压缩到紧凑的 **32 维潜空间**。通过相对位移编码和 ResMLP 网络，构建了一个语义判别性强、物理边界清晰的特征空间。
*   **MLP-Mixer 解码器**：放弃了计算量大的 Transformer 注意力机制，改用超轻量级的 **MLP-Mixer**。它通过交替进行空间和通道混合，实现了线性计算复杂度，极大地降低了 FLOPs。
*   **潜空间漂移损失（Latent-space Drifting Loss）**：
    *   引入**漂移模型（Drifting Models）**理论，在潜空间中定义引力（吸引生成特征靠近专家流形）和斥力（防止模式塌陷，增加多样性）。
    *   通过调节引导尺度 $\alpha$，模型可以生成从保守到主动的多种轨迹。
*   **PCA 解码器**：最后通过预训练的 PCA 算子将潜特征还原为物理坐标轨迹，确保运动学上的合理性。

### 3. 实验设计
*   **数据集与场景**：使用大规模自动驾驶基准 **nuPlan**，涵盖波士顿、匹兹堡、拉斯维加斯和新加坡的真实驾驶数据。
*   **Benchmark**：主要在 **Test14** 和更具挑战性的 **Test14-hard** 划分上进行评估。
*   **评估设置**：
    *   **非反应式（Non-Reactive, NR）**：背景车辆按日志回放。
    *   **反应式（Reactive, R）**：背景车辆使用 IDM 模型进行交互。
*   **对比方法**：包括规则/混合方法（IDM, PDM-Closed）、学习型方法（PlanTF, PLUTO）以及生成式方法（Diffusion Planner）。

### 4. 资源与算力
*   **硬件环境**：推理性能测试在 **NVIDIA RTX 4090 GPU** 上完成。
*   **训练细节**：VAE 特征提取器预训练了 200 个 epoch。
*   **性能指标**：端到端推理延迟为 **10.1 ms**，核心生成模块仅需 **4.4 ms**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了广泛的定量对比（Test14 全集）和定性分析。
*   **消融与验证**：
    *   对比了绝对坐标空间、PCA 空间与 VAE 潜空间的特征分布（通过 t-SNE 可视化）。
    *   分析了不同引导尺度 $\alpha$ 对轨迹多样性的影响。
    *   对比了带与不带细化模块（Refinement）的性能差异。
*   **客观性**：实验涵盖了反应式和非反应式两种闭环仿真环境，能够较为全面地反映模型在真实交互中的表现。

### 6. 主要结论与发现
*   **性能领先**：MISTY 在 Test14-hard 反应式设置下取得了 **82.21** 的高分，刷新了 SOTA 纪录。
*   **极高效率**：运行速度超过 **99 FPS**，比迭代式扩散规划器快了一个数量级。
*   **泛化能力**：模型能够合成专家数据中罕见的**主动超车**等行为，证明了漂移机制在探索新颖安全决策方面的有效性。
*   **特征空间优势**：VAE 潜空间的类间/类内距离比比 PCA 空间提升了 23 倍，为生成任务提供了极佳的结构化先验。

### 7. 优点（亮点）
*   **单步推理实现 SOTA**：打破了“单步生成必输给迭代生成”的固有印象。
*   **计算效率极高**：MLP-Mixer 的引入使得模型非常适合车载边缘设备的部署。
*   **主动性强**：通过漂移损失函数的设计，模型不再只是简单的模仿学习，而是具备了处理长尾场景（如路障绕行）的主动规划能力。

### 8. 不足与局限
*   **平滑度略逊**：单步生成的轨迹平滑度在原始输出下略低于多步迭代模型，需要轻量级的细化模块来补偿舒适度指标。
*   **长尾动作学习难**：在极端紧急制动或精确停车等样本极稀少的场景下，单步模型仍存在一定的控制不稳定性。
*   **依赖预训练空间**：模型的表现高度依赖于 VAE 潜空间的构建质量，若训练数据分布存在严重偏差，潜空间的物理约束可能失效。

（完）
