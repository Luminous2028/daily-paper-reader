---
title: Accelerating trajectory optimization with Sobolev-trained diffusion policies
title_zh: 使用基于 Sobolev 训练的扩散策略加速轨迹优化
authors: "Théotime Le Hellard, Franki Nguimatsia Tiofack, Quentin Le Lidec, Justin Carpentier"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19011v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 用于热启动轨迹优化的扩散策略
tldr: 针对轨迹优化（TO）求解速度慢且依赖初始值的问题，本文提出了一种利用Sobolev训练的扩散策略进行预热启动的方法。通过结合TO求解器提供的反馈增益，利用一阶损失函数训练扩散模型，有效解决了模仿学习中的复合误差问题。实验表明，该方法仅需少量数据即可显著提升求解效率，将TO耗时降低2至20倍，并减少了推理延迟。
selection_source: fresh_fetch
motivation: 传统的轨迹优化求解器对初始值敏感且计算开销大，而现有的模仿学习策略在预热启动时容易因复合误差导致性能下降。
method: 提出一种基于Sobolev学习的扩散策略，利用梯度下降TO求解器提供的一阶反馈增益信息来增强策略的鲁棒性。
result: 该方法在减少训练数据需求的同时，将轨迹优化速度提升了2到20倍，并降低了扩散模型的推理步数。
conclusion: 结合一阶信息的Sobolev训练能显著提升扩散策略在轨迹优化预热启动中的表现，实现更高效、更稳健的机器人运动规划。
---

## 摘要
轨迹优化 (TO) 求解器利用已知的系统动力学，通过迭代改进来计算局部最优轨迹。其缺点是每个新的问题实例都是独立求解的；因此，收敛速度和所获解的质量取决于所提供的初始轨迹。为了提高效率，一种自然的方法是使用学习到的策略生成的初始猜测来热启动 TO，该策略是在求解器先前生成的轨迹上训练的。基于扩散的策略最近作为具有表现力的模仿学习模型出现，使其成为该角色的有力候选者。然而，一个违反直觉的挑战来自于 TO 演示的局部最优性：当策略展开时，微小的非最优偏差可能会将其推入训练数据中未呈现的情境，从而在长时界内引发复合误差。在这项工作中，我们专注于为同时提供反馈增益的基于梯度的 TO 求解器进行基于学习的热启动。利用这一特性，我们推导出了一个一阶损失，用于结合轨迹和反馈增益对基于扩散的策略进行 Sobolev 学习。通过全面的实验，我们证明了所得策略避免了复合误差，因此可以从极少数轨迹中学习，以提供初始猜测，从而将求解时间缩短 2 到 20 倍。结合一阶信息使得能够以更少的扩散步骤进行预测，从而降低了推理延迟。

## Abstract
Trajectory Optimization (TO) solvers exploit known system dynamics to compute locally optimal trajectories through iterative improvements. A downside is that each new problem instance is solved independently; therefore, convergence speed and quality of the solution found depend on the initial trajectory proposed. To improve efficiency, a natural approach is to warm-start TO with initial guesses produced by a learned policy trained on trajectories previously generated by the solver. Diffusion-based policies have recently emerged as expressive imitation learning models, making them promising candidates for this role. Yet, a counterintuitive challenge comes from the local optimality of TO demonstrations: when a policy is rolled out, small non-optimal deviations may push it into situations not represented in the training data, triggering compounding errors over long horizons. In this work, we focus on learning-based warm-starting for gradient-based TO solvers that also provide feedback gains. Exploiting this specificity, we derive a first-order loss for Sobolev learning of diffusion-based policies using both trajectories and feedback gains. Through comprehensive experiments, we demonstrate that the resulting policy avoids compounding errors, and so can learn from very few trajectories to provide initial guesses reducing solving time by $2\times$ to $20 \times$. Incorporating first-order information enables predictions with fewer diffusion steps, reducing inference latency.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合 Sobolev 训练和扩散策略的方法，旨在加速机器人轨迹优化（TO）过程。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：轨迹优化（TO）求解器（如 iLQR、DDP）虽然强大，但对初始猜测（Initial Guess）高度敏感，且每个新问题都需从头求解，计算开销大。
*   **研究背景**：利用模仿学习（IL）训练策略来“热启动”TO 是一种常见思路。然而，TO 生成的演示数据是局部最优的，缺乏应对偏差的“恢复”示例。这导致传统策略在执行时会产生**复合误差（Compounding Errors）**：微小偏差使系统进入未见过的状态空间，最终导致任务失败。
*   **核心含义**：论文试图通过引入一阶导数信息（反馈增益），利用 Sobolev 空间学习理论增强扩散策略的鲁棒性，使其能以极少量的数据实现高效、稳定的热启动。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用梯度下降式 TO 求解器在收敛时自动提供的**反馈增益（Feedback Gains）**，将其作为一阶监督信号加入扩散模型的训练损失函数中。
*   **关键技术细节**：
    *   **Sobolev 扩散损失**：在标准扩散模型损失（匹配轨迹 $\tau_0$）的基础上，增加了一个一阶项，要求模型预测的轨迹对初始状态的雅可比矩阵（Jacobian）与求解器提供的反馈增益链式求导后的结果一致。
    *   **随机投影（Stochastic Sobolev）**：为避免计算高昂的 Hessian 矩阵，采用随机向量投影技术，将高维导数匹配简化为标量投影匹配。
    *   **交替迭代算法（Interplay Loop）**：类似于 DAgger，算法交替进行“数据采集”和“策略训练”。在采集阶段，同时尝试“冷启动”和“策略热启动”TO，保留更好的轨迹；在训练阶段，使用 Sobolev 损失更新扩散策略。
    *   **扩散模型参数化**：采用预测 $\tau_0$（原始轨迹）而非噪声 $\epsilon$ 的参数化方式，实验证明这在轨迹生成任务中更稳定。

### 3. 实验设计
*   **实验场景**：
    1.  **倒立摆（单/双）**：测试力矩控制下的非线性系统摆起任务。
    2.  **UR5 机械臂**：设置了 4 种变体（固定/随机初始位姿，固定/随机目标点），测试运动学规划。
    3.  **四旋翼无人机**：包括无障碍物和带有 9 个柱状障碍物的复杂环境。
*   **对比基准（Benchmarks）**：
    *   **TO alone**：无热启动的原始求解器。
    *   **MLP / Sob+MLP**：传统的直接策略网络及其 Sobolev 版本。
    *   **Diff**：标准的扩散策略（不含 Sobolev 损失，类似于 DiffuSolve）。
    *   **Sob+Diff**：本文提出的方法。

### 4. 资源与算力
*   **硬件环境**：所有实验均在一台笔记本电脑上完成，配置为 **Intel Core i9-13950HX CPU** 和 **Nvidia RTX2000 ADA GPU**。
*   **训练细节**：使用了单向量投影（$n_{proj}=1$），使得 Sob+Diff 的单次训练迭代耗时仅为标准扩散模型的 2 倍左右。扩散步数 $K$ 设置为极小的 5 步，以降低推理延迟。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 类机器人任务，共 8 个变体。
*   **充分性评价**：
    *   **统计严谨性**：所有结果均基于 **5 个随机种子**取平均值，并给出了置信区间。
    *   **消融与灵敏度**：详细分析了训练轨迹数量（从 3 条到 100 条不等）、训练轮数（Epochs）、预测时界（Horizon）以及动作长度（Action Length）对性能的影响。
    *   **客观性**：实验不仅对比了最终成本（Cost），还对比了 TO 迭代次数和实际推理/求解时间，证明了方法在效率上的真实提升。

### 6. 主要结论与发现
*   **样本效率极高**：Sob+Diff 仅需 3-5 条轨迹即可在简单任务中胜出，在复杂任务中所需数据量比标准扩散模型少 5 到 10 倍。
*   **大幅加速求解**：通过提供高质量初始猜测，将 TO 求解器的迭代次数显著降低，整体求解速度提升 **2 倍至 20 倍**。
*   **解决复合误差**：Sobolev 训练使策略具备了更好的局部线性近似能力，即使在长时界预测中也能保持稳定，避免了扩散策略常见的漂移问题。
*   **推理延迟低**：由于一阶信息的引入，模型可以用更少的扩散步数（如 5 步）达到高精度，降低了实时控制的延迟。

### 7. 优点与亮点
*   **理论与工程结合**：巧妙地将优化理论中的反馈增益与生成式 AI 中的扩散模型结合，解决了模仿学习在 TO 领域应用的核心痛点。
*   **实用性强**：不需要额外的专家演示，直接利用求解器副产品（导数）进行增强，且对算力要求低。
*   **鲁棒性**：在双倒立摆等混沌系统中表现出极强的稳定性，而传统 MLP 或标准扩散模型在同等数据量下极易发散。

### 8. 不足与局限
*   **约束处理**：Sobolev 损失项目前主要针对目标函数梯度，虽然在带障碍物的实验中表现尚可，但缺乏显式的硬约束保证机制。
*   **任务泛化限制**：实验主要集中在目标点可达性任务上，对于涉及频繁接触（Contact-rich）或拓扑结构剧烈变化的复杂任务，其导数信息的连续性可能受损。
*   **求解器依赖**：该方法依赖于能提供一阶导数的梯度下降式求解器，对于非梯度类优化算法不适用。

（完）
