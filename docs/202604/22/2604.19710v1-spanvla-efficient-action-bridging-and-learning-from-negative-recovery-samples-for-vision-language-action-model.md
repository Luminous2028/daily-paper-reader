---
title: "SpanVLA: Efficient Action Bridging and Learning from Negative-Recovery Samples for Vision-Language-Action Model"
title_zh: SpanVLA：面向视觉-语言-动作模型的高效动作桥接与负样本-恢复样本学习
authors: "Zewei Zhou, Ruining Yang, Xuewei, Qi, Yiluan Guo, Sherry X. Chen, Tao Feng, Kateryna Pistunova, Yishan Shen, Lili Su, Jiaqi Ma"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19710v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 用于自动驾驶轨迹规划的流匹配动作专家模型
tldr: SpanVLA是一个高效的端到端自动驾驶框架，旨在解决现有VLA模型推理延迟高和鲁棒性不足的问题。它结合了自回归推理与流匹配动作专家，通过高效桥接机制利用VLM的视觉推理引导轨迹规划，显著降低了推理时间。此外，研究引入了基于GRPO的后训练方法，使模型能从负面样本中学习恢复行为，并发布了mReasoning数据集。实验证明其在NAVSIM基准上具有极具竞争力的性能和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的视觉-语言-动作（VLA）模型在自动驾驶中面临自回归生成导致的推理延迟高以及在复杂场景下鲁棒性不足的挑战。
method: 提出SpanVLA框架，通过流匹配策略加速动作生成，并利用基于GRPO的后训练方法结合负面-恢复样本提升模型的纠错与避障能力。
result: 在NAVSIM（v1和v2）基准测试中，SpanVLA展现了优异的规划性能，并在推理效率和处理复杂推理场景的鲁棒性上表现突出。
conclusion: SpanVLA通过结合高效的动作生成机制与负面样本学习，为构建实时且鲁棒的自动驾驶VLA模型提供了有效的解决方案。
---

## 摘要
视觉-语言-动作（VLA）模型为利用世界知识和推理能力提供了一种极具前景的自动驾驶范式，特别是在长尾场景中。然而，现有的 VLA 模型在使用自回归生成框架进行动作生成时，往往面临高延迟的问题，且鲁棒性有限。在本文中，我们提出了 SpanVLA，这是一种新型的端到端自动驾驶框架，集成了自回归推理和流匹配（flow-matching）动作专家。首先，SpanVLA 引入了一个高效的桥接机制，利用视觉语言模型（VLM）的视觉和推理引导，通过以历史轨迹初始化为条件的流匹配策略来高效规划未来轨迹，从而显著缩短了推理时间。其次，为了进一步提高 SpanVLA 模型的性能和鲁棒性，我们提出了一种基于 GRPO 的后训练方法，使 VLA 模型不仅能从正向驾驶样本中学习，还能学习如何避免典型的负面行为并学习恢复行为。我们还进一步引入了 mReasoning，这是一个新的真实世界驾驶推理数据集，专注于复杂的、需要推理的场景以及负样本-恢复样本。在 NAVSIM（v1 和 v2）上的广泛实验证明了 SpanVLA 模型具有竞争力的性能。此外，在不同场景下的定性结果突显了我们模型的规划性能和鲁棒性。

## Abstract
Vision-Language-Action (VLA) models offer a promising autonomous driving paradigm for leveraging world knowledge and reasoning capabilities, especially in long-tail scenarios. However, existing VLA models often struggle with the high latency in action generation using an autoregressive generation framework and exhibit limited robustness. In this paper, we propose SpanVLA, a novel end-to-end autonomous driving framework, integrating an autoregressive reasoning and a flow-matching action expert. First, SpanVLA introduces an efficient bridge to leverage the vision and reasoning guidance of VLM to efficiently plan future trajectories using a flow-matching policy conditioned on historical trajectory initialization, which significantly reduces inference time. Second, to further improve the performance and robustness of the SpanVLA model, we propose a GRPO-based post-training method to enable the VLA model not only to learn from positive driving samples but also to learn how to avoid the typical negative behaviors and learn recovery behaviors. We further introduce mReasoning, a new real-world driving reasoning dataset, focusing on complex, reasoning-demanding scenarios and negative-recovery samples. Extensive experiments on the NAVSIM (v1 and v2) demonstrate the competitive performance of the SpanVLA model. Additionally, the qualitative results across diverse scenarios highlight the planning performance and robustness of our model.

---

## 论文详细总结（自动生成）

以下是对论文《SpanVLA: Efficient Action Bridging and Learning from Negative-Recovery Samples for Vision-Language-Action Model》的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：现有的视觉-语言-动作（VLA）模型在自动驾驶中虽具备强大的推理和泛化能力，但面临两大挑战：
    1.  **高延迟**：传统的自回归（Autoregressive）解码方式生成动作序列时，耗时随序列长度线性增长，难以满足自动驾驶的高频控制需求。
    2.  **鲁棒性不足**：现有模型大多仅通过模仿学习（Imitation Learning）从专家（正向）样本中学习，缺乏对错误行为的辨识能力以及从危险状态中恢复的能力。
*   **核心目标**：构建一个既能保持VLM推理能力，又能实现高效动作生成，且具备强鲁棒性的端到端自动驾驶框架。

### 2. 方法论
SpanVLA 框架由 **VLM 主干**、**高效动作桥接器** 和 **流匹配动作专家** 组成，并采用两阶段训练策略。
*   **核心思想**：将“慢思考”（VLM 链式推理）与“快反应”（流匹配动作生成）结合，并引入负面/恢复样本进行强化学习微调。
*   **关键技术细节**：
    *   **高效动作桥接（Action Bridging）**：不同于仅提取最后一层特征，SpanVLA 从 VLM 的多个稀疏层（Sparse Layers）提取 KV Cache 特征，捕捉从原始视觉到高级推理的多粒度信息。
    *   **流匹配动作专家（Flow-matching Action Expert）**：采用流匹配策略生成连续轨迹。创新点在于**历史轨迹初始化**，即不从纯高斯噪声开始去噪，而是以历史轨迹嵌入为起点学习向未来轨迹的转换，提高了生成质量和效率。
    *   **自适应推理机制**：模型可根据场景复杂度动态选择“快速思考”（直接输出动作）或“慢速思考”（输出 CoT 推理+动作）。
    *   **基于 GRPO 的强化学习微调（RFT）**：使用群体相对策略优化（GRPO），设计了**负面行为惩罚**（针对子优化轨迹）和**恢复行为奖励**（针对专家纠偏轨迹），使模型学会“避坑”与“自救”。

### 3. 实验设计
*   **数据集**：
    *   **nuPlan (Open-Scene)**：包含 100K 个场景。
    *   **mReasoning (新发布)**：包含 30K 个具有复杂交互和 CoT 标注的场景，以及 6K 个专门的负面-恢复（Negative-Recovery）样本对。
*   **Benchmark**：NAVSIM v1 (navtest) 和 NAVSIM v2 (navtest & navhard)。
*   **对比方法**：
    *   传统端到端方法：TransFuser, Hydra-MDP, DiffusionDrive 等。
    *   VLA 相关方法：DriveVLA, AutoVLA, ReCogDrive 等。
*   **评估指标**：PDMS（预测驾驶模型得分）及其扩展版本 EPDMS，涵盖安全性、舒适性、效率等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练细节**：
    *   **SFT 阶段**：使用 FSDP（全分片数据并行）技术。VLM 主干（Qwen2.5VL-3B）训练时每张 GPU 的 Batch Size 为 1，梯度累积步数为 4。
    *   **RFT 阶段**：采用 LoRA 适配器进行高效微调，学习率为 $3 \times 10^{-5}$，群体采样大小（Group Size）为 64。
*   **推理效率**：在生成 50 个路点时，SpanVLA 的轨迹生成耗时仅为 0.08s，相比 AutoVLA 的 1.72s 降低了约 **74%**。

### 5. 实验数量与充分性
*   **实验规模**：论文在 NAVSIM v1 和 v2 两个主流基准上进行了全面测试，并针对 navhard（困难场景）进行了专门评估。
*   **消融实验**：
    *   验证了**历史初始化**对流匹配性能的提升。
    *   对比了不同**稀疏层提取方案**对精度与速度的影响。
    *   详细探讨了 **RFT 数据配比**（正/负/恢复样本比例）对最终得分的影响。
*   **客观性**：实验设置了固定的随机种子，并遵循 NAVSIM 官方的评估协议，对比了近年来最先进的多种模型，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：SpanVLA 在 NAVSIM v1 和 v2 上均达到了 SOTA 性能（如 v1 PDMS 达到 90.3）。
*   **效率飞跃**：通过动作桥接和流匹配，显著解决了 VLA 模型推理延迟的瓶颈，轨迹生成速度提升数倍。
*   **鲁棒性增强**：引入负面-恢复样本的 RFT 显著提升了模型在长尾场景（如施工区、强行切入）中的表现，模型学会了在错误发生前主动避让或在偏离后快速修正。

### 7. 优点
*   **高效性**：流匹配+历史初始化的设计在保证精度的同时极大压缩了推理时间。
*   **数据创新**：提出的 mReasoning 数据集填补了真实世界负面-恢复样本的空白，对社区具有重要价值。
*   **训练策略**：将 GRPO 应用于自动驾驶轨迹优化，并结合负面惩罚项，提供了一种比单纯模仿学习更有效的策略对齐方式。

### 8. 不足与局限
*   **实时性仍有差距**：虽然推理速度大幅提升，但目前 1.5 Hz 的整体频率（含 VLM 推理）距离真正的实时部署（通常需 10Hz 以上）仍有距离。
*   **硬件优化缺失**：论文提到尚未进行 TensorRT 等硬件级加速优化。
*   **奖励函数设计**：负面惩罚和恢复奖励的权重及阈值对性能较敏感，可能需要针对不同城市或驾驶风格进行精细调优。

（完）
