---
title: "AnchorVLA: Anchored Diffusion for Efficient End-to-End Mobile Manipulation"
title_zh: AnchorVLA：用于高效端到端移动操作的锚定扩散模型
authors: "Jia Syuen Lim, Zhizhen Zhang, Peter Bohm, Brendan Tidd, Zi Huang, Yadan Luo"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01567v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 用于多模态动作分布和操作不确定性的扩散策略
tldr: 针对移动操作中扩散策略推理慢和动作分块导致的漂移问题，本文提出AnchorVLA。它结合轻量级VLA骨干网络与锚定扩散动作头，通过在锚定轨迹附近进行局部去噪，大幅减少推理开销并保留多模态特性。同时引入残差修正模块实现高频自纠偏，在保持低延迟的同时显著提升了复杂任务下的成功率和鲁棒性。
selection_source: fresh_fetch
motivation: 传统的扩散策略在移动操作中面临推理延迟高和动作分块导致的累积漂移问题，难以兼顾多模态分布与实时闭环控制。
method: 提出一种锚定扩散机制，在锚定轨迹附近进行截断式局部去噪，并结合轻量级残差模块进行每步的高频自纠偏。
result: 在多种移动操作任务中，AnchorVLA 在面对干扰和分布偏移时表现出更高的成功率和稳定性，且具备极低的推理延迟。
conclusion: AnchorVLA 通过局部去噪和实时修正，成功实现了高效、鲁棒且具备多模态能力的端到端移动机器人控制。
---

## 摘要
移动操作中的一个核心挑战是在执行过程中保持反应性的同时，保留多种合理的动作模型。在杂乱场景中，抓取瓶子通常有多种有效方式。鲁棒的行为取决于在场景演变时保持这种动作多样性并维持反应性。扩散策略（Diffusion policies）因其能够建模多模态动作分布而非坍缩至单一解而备受关注。但在实践中，完整的迭代去噪在控制时的计算成本很高。动作分块（Action chunking）虽有助于分摊推理开销，但也会导致部分开环行为，使得微小的失配累积成漂移。我们提出了 AnchorVLA，这是一种基于扩散的移动操作 VLA 策略。其核心见解是：当采样从合理方案流形附近开始时，无需进行大量的去噪即可恢复多模态的有效动作。AnchorVLA 将轻量级 VLA 适配主干与锚定扩散动作头相结合，利用截断扩散调度在锚定轨迹周围进行局部去噪。这在保留多模态动作生成的同时，降低了闭环控制的推理成本。至关重要的是，为减轻分块诱发的漂移，我们通过轻量级残差修正模块引入了测试时自校正机制，在执行过程中进行高频的逐步调整。在多种移动操作任务中，AnchorVLA 在保持低延迟推理的同时，显著提升了在扰动和分布偏移下的成功率与稳定性。源代码已开源至 https://github.com/jason-lim26/AnchorVLA。

## Abstract
A central challenge in mobile manipulation is preserving multiple plausible action models while remaining reactive during execution. A bottle in a cluttered scene can often be approached and grasped in multiple valid ways. Robust behavior depends on preserving this action diversity while remaining reactive as the scene evolves. Diffusion policies are appealing because they model multimodal action distributions rather than collapsing to one solution. But in practice, full iterative denoising is costly at control time. Action chunking helps amortize inference, yet it also creates partially open-loop behavior, allowing small mismatches to accumulate into drift. We present AnchorVLA, a diffusion-based VLA policy for mobile manipulation built on the core insight that when sampling begins near a plausible solution manifold, extensive denoising is unnecessary to recover multimodal, valid actions. AnchorVLA combines a lightweight VLA adaptation backbone with an anchored diffusion action head, which denoises locally around anchor trajectories using a truncated diffusion schedule. This retains multimodal action generation while reducing inference cost for closed-loop control. Crucially, to mitigate chunking-induced drift, we introduce a test-time self-correction mechanism via a lightweight residual correction module that makes high-frequency, per-step adjustments during rollout. Across diverse mobile manipulation tasks, AnchorVLA improves success and stability under disturbances and distribution shifts while maintaining low-latency inference. The source code is made available at https://github.com/jason-lim26/AnchorVLA.

---

## 论文详细总结（自动生成）

### AnchorVLA：用于高效端到端移动操作的锚定扩散模型论文总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：移动操作（Mobile Manipulation）要求机器人同时处理高层语义推理、长程导航和复杂的物理交互。其核心挑战在于**多模态性**（同一指令有多种有效执行路径）和**时空耦合**。
*   **现有痛点**：
    *   **确定性回归模型（如 L1 回归）**：在面对多模态分布时会产生“模式平均”现象，导致生成的动作在物理上不可行。
    *   **标准扩散策略（Diffusion Policy）**：虽然能捕捉多模态，但全迭代去噪计算成本高，推理延迟大。
    *   **动作分块（Action Chunking）**：虽能分摊推理开销，但本质上是部分开环执行，微小的状态误差会随时间累积成严重的“执行漂移”。
*   **核心目标**：设计一个既能建模多模态分布、又具备高效推理能力，且能抑制分块执行漂移的端到端 VLA 策略。

#### 2. 方法论
AnchorVLA 由轻量级 VLA 适配器骨干、锚定扩散动作头和残差修正模块组成：
*   **锚定截断扩散（Anchored Truncated Diffusion）**：
    *   **轨迹词汇表**：从专家演示中通过 K-Means 聚类提取 $M$ 个代表性的动作轨迹作为“锚点”（Anchors）。
    *   **截断去噪**：采样不再从纯高斯噪声开始，而是从锚点轨迹附近开始。通过缩短去噪步数（如仅需 5-10 步），在保留多模态表达能力的同时大幅提升推理速度。
*   **锚定评分头（Anchor Scoring Head）**：
    *   模型同时对多个锚点进行局部去噪，评分头根据当前视觉上下文和指令，为每个去噪后的候选轨迹预测置信度分数，从而选择最优轨迹。
*   **测试时自校正（Test-Time Self-Correction）**：
    *   引入一个极轻量级的**残差修正模块（MLP）**。在执行长动作块的每一个子步骤中，根据实时观测值对预定义的宏观动作进行微调（预测 $\Delta a$），实现高频闭环反馈，抵消开环漂移。

#### 3. 实验设计
*   **仿真环境**：使用 **ManiSkill-HAB (MS-HAB)** 平台，基于 ReplicaCAD 场景和 YCB 物体集。
*   **任务设置**：涵盖 6 类移动操作任务：抓取苹果/碗、放置苹果/碗、开冰箱门、关抽屉。
*   **真机部署**：在 **Unitree Go2 四足机器人**（配备 SO101 机械臂）上进行验证，测试其在足式底盘振动和漂移下的鲁棒性。
*   **对比基准（Baselines）**：
    *   **模仿学习框架**：ACT, Diffusion Policy (DP)。
    *   **3D/大模型策略**：DP3, $\pi_0$, RDT-1B。
    *   **移动操作 SOTA**：AC-DiT（自适应协调扩散 Transformer）。

#### 4. 资源与算力
*   **模型规模**：骨干网络采用 **Qwen-2.5 (0.5B)**，使用 LoRA（Rank=64）进行参数高效微调。
*   **训练细节**：Batch size 为 16，训练进行 100k 个梯度步。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）及总训练时长，但提到残差模块仅增加 57K 参数，推理频率可达 53-90Hz，显著降低了 TFLOPs 消耗（相比逐帧查询降低约 80%）。

#### 5. 实验数量与充分性
*   **实验规模**：在仿真中对每个任务进行了 3 次独立试验，每次试验包含 100 个未见过的随机回合，统计平均成功率和标准差。
*   **消融实验**：
    *   验证了锚定先验在低去噪步数下的必要性（10 步去噪时，无锚点模型成功率为 0，AnchorVLA 为 42.9%）。
    *   验证了残差修正模块对成功率的提升（提升约 9.3%）。
    *   分析了动作分块长度 $H$ 对性能的影响，证明了模型在长分块下的稳定性。
*   **充分性评价**：实验设计较为全面，涵盖了从 2D 到 3D、从确定性到生成式的多种主流基准，且包含真机压力测试，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能领先**：AnchorVLA 在 MS-HAB 仿真中达到 **64.0%** 的平均成功率，超过了依赖点云输入的 AC-DiT（55.6%）和 RDT（42.9%）。
*   **高效性**：通过锚定截断机制，在仅使用 5 步去噪时即可达到基准模型 50 步去噪的性能，推理频率提升 1.48 倍。
*   **鲁棒性**：即使在动作分块长度增加（如 $H=5$ 或 $H=8$）时，性能下降远比 L1 回归模型缓慢，证明了其对长程开环执行的耐受力。

#### 7. 优点（亮点）
*   **创新的去噪策略**：首次将锚定截断扩散引入移动操作，巧妙平衡了多模态表达与推理延迟。
*   **双层控制架构**：通过“宏观扩散规划 + 微观残差修正”的组合，解决了动作分块带来的累积误差问题。
*   **计算效率极高**：通过长分块执行和间歇性 VLA 查询，大幅降低了端到端系统的算力需求，适合移动设备部署。

#### 8. 不足与局限
*   **数据依赖性**：作为模仿学习方法，其性能上限受限于专家演示数据的覆盖范围，在完全未见过的分布外（OOD）场景下可能失效。
*   **真机任务复杂度**：虽然进行了真机实验，但任务（开抽屉、提包）相对单一，尚未在极度动态或高度杂乱的真实环境中进行大规模验证。
*   **锚点聚类**：锚点轨迹是离线聚类得到的，对于极其多样化的任务，可能需要更复杂的动态锚点生成机制。

（完）
