---
title: "WOMBET: World Model-based Experience Transfer for Robust and Sample-efficient Reinforcement Learning"
title_zh: WOMBET：基于世界模型的经验迁移，助力鲁棒且高效采样的强化学习
authors: "Mintae Kim, Koushil Sreenath"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08958v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 在规划中过滤具有低认知不确定性的轨迹
tldr: 针对机器人强化学习中数据采集成本高的问题，本文提出WOMBET框架，通过世界模型实现经验迁移。该方法在源任务中学习模型，利用不确定性惩罚规划生成并筛选高质量数据，并在目标任务中通过自适应采样进行在线微调。WOMBET在理论上提供了性能保证，并在实验中显著提升了样本效率和最终表现，证明了联合优化数据生成与迁移的有效性。
selection_source: fresh_fetch
motivation: 现有的离线到在线强化学习通常依赖预设的固定数据集，缺乏针对迁移任务主动生成可靠数据的有效机制。
method: 利用源任务学习的世界模型进行不确定性惩罚规划以生成数据，并通过自适应采样策略在目标任务中进行在线微调。
result: 实验表明，WOMBET在多个连续控制基准任务上的样本效率和最终性能均显著优于现有的强基准算法。
conclusion: 联合优化数据生成与迁移过程能够有效缓解分布偏移，是实现鲁棒且高效强化学习的关键路径。
---

## 摘要
机器人领域的强化学习（RL）常受限于数据采集的成本和风险，这促使了从源任务到目标任务的经验迁移。离线到在线（Offline-to-online）RL 利用先验数据，但通常假设数据集是给定且固定的，并未解决如何生成可靠的迁移数据。我们提出了“基于世界模型的经验迁移”（WOMBET），这是一个联合生成并利用先验数据的框架。WOMBET 在源任务中学习一个世界模型，并通过不确定性惩罚规划生成离线数据，随后筛选出具有高回报和低认知不确定性的轨迹。接着，它在目标任务中通过离线和在线数据之间的自适应采样进行在线微调，实现了从先验驱动的初始化到任务特定适配的平稳过渡。我们证明了不确定性惩罚目标提供了真实回报的下界，并推导出了捕捉分布失配和近似误差的有限样本误差分解。实验表明，WOMBET 在连续控制基准测试中优于强基准方法，提升了采样效率和最终性能，证明了联合优化数据生成与迁移的益处。

## Abstract
Reinforcement learning (RL) in robotics is often limited by the cost and risk of data collection, motivating experience transfer from a source task to a target task. Offline-to-online RL leverages prior data but typically assumes a given fixed dataset and does not address how to generate reliable data for transfer. We propose \textit{World Model-based Experience Transfer} (WOMBET), a framework that jointly generates and utilizes prior data. WOMBET learns a world model in the source task and generates offline data via uncertainty-penalized planning, followed by filtering trajectories with high return and low epistemic uncertainty. It then performs online fine-tuning in the target task using adaptive sampling between offline and online data, enabling a stable transition from prior-driven initialization to task-specific adaptation. We show that the uncertainty-penalized objective provides a lower bound on the true return and derive a finite-sample error decomposition capturing distribution mismatch and approximation error. Empirically, WOMBET improves sample efficiency and final performance over strong baselines on continuous control benchmarks, demonstrating the benefit of jointly optimizing data generation and transfer.

---

## 论文详细总结（自动生成）

这篇论文提出了 **WOMBET**（基于世界模型的经验迁移），旨在解决机器人强化学习中数据采集成本高、安全性差以及现有迁移学习方法依赖固定数据集的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：强化学习（RL）在机器人领域面临样本效率低和安全风险大的挑战。虽然“离线到在线”（Offline-to-Online）RL 可以利用先验数据加速学习，但现有方法通常假设离线数据集是预先给定的，忽略了**如何主动生成高质量、可迁移的先验数据**。
*   **核心问题**：如何从源任务（Source Task）中生成既具有高回报又具有高可靠性（低模型误差）的数据，并将其有效地迁移到目标任务（Target Task）中，同时缓解分布偏移（Distributional Shift）带来的性能崩溃。

### 2. 方法论：WOMBET 框架
WOMBET 采用了一种联合优化数据生成与迁移的迭代流程，核心步骤如下：
*   **不确定性感知的数据生成**：
    *   在源任务中学习一个概率集成世界模型（Probabilistic Ensemble World Model）。
    *   使用**不确定性惩罚的 MPC（模型预测控制）**进行规划。公式化目标为：$\tilde{r}(s, a) = r(s, a) - \lambda u(s, a)$，其中 $u(s, a)$ 是集成模型预测的认知不确定性。这确保了生成的轨迹在模型预测准确的区域内。
*   **双准则过滤（Dual-Criterion Filtering）**：
    *   并非所有生成的轨迹都用于迁移。WOMBET 仅保留满足以下两个条件的轨迹：**高累积回报**（保证任务相关性）和**低平均认知不确定性**（保证数据真实性）。
*   **自适应混合数据训练**：
    *   在目标任务中，通过自适应采样比例 $\alpha$ 混合离线数据 $D_S$ 和在线数据 $D_T$。
    *   **自适应规则**：根据评论家网络（Critic）的 TD 误差动态调整 $\alpha$。当 TD 误差较大（欠拟合）时，增加离线数据比例以稳定训练；随着模型收敛，逐渐转向在线数据以减少分布偏移。
*   **理论保证**：论文证明了不确定性惩罚目标是真实回报的下界，并推导了包含分布失配和有限样本近似误差的性能界限。

### 3. 实验设计
*   **实验场景**：基于 **MuJoCo** 连续控制基准测试（如 HalfCheetah, Hopper, Ant, Walker2d 等）。
*   **对比基准（Benchmarks）**：
    *   **从零开始的在线 RL**：SAC, PPO, TD3。
    *   **纯离线 RL**：BC, IQL, CQL, MOPO, COMBO（验证仅靠生成数据是否足够）。
    *   **离线到在线 RL**：RLPD 等强基准（验证迁移机制的优越性）。
*   **消融实验**：验证了“双准则过滤”和“自适应采样”对最终性能的贡献。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 LayerNorm 稳定 Critic 网络，并采用了集成 Critic 结构。实验针对每个环境运行了 5 个随机种子以保证统计显著性。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同的 MuJoCo 任务上进行了对比，涵盖了样本效率、最终性能、零射迁移能力等多个维度。
*   **充分性评价**：实验设计较为充分。不仅对比了主流的在线和离线算法，还专门设计了消融实验来拆解 WOMBET 的各个组件。通过 5 个种子的标准差阴影图展示了结果的稳定性，客观性较强。

### 6. 主要结论与发现
*   **联合优化的必要性**：联合优化数据生成和迁移过程显著优于使用静态数据集的方法。
*   **样本效率提升**：WOMBET 在达到相同性能时，所需的真实环境交互次数通常不到基准方法的一半。
*   **鲁棒性**：通过不确定性惩罚和双准则过滤，WOMBET 有效避免了模型偏见（Model Bias）导致的性能崩溃，实现了从离线初始化到在线适配的平稳过渡。

### 7. 优点与亮点
*   **闭环框架**：打破了离线数据固定的假设，将世界模型的规划能力与无模型 RL 的微调能力有机结合。
*   **可靠性控制**：引入认知不确定性作为过滤准则，从源头上解决了模型生成数据可能存在的“虚假高分”问题。
*   **自适应性**：基于 TD 误差的动态采样策略非常直观且有效，解决了离线/在线数据权重难以手动调优的痛点。

### 8. 不足与局限
*   **任务相似性假设**：该方法假设源任务和目标任务共享动力学（Dynamics），仅奖励函数或初始状态不同。如果动力学发生剧变，世界模型的迁移效果可能会大打折扣。
*   **计算开销**：虽然提升了样本效率（减少了环境交互），但学习集成世界模型并在规划时计算不确定性会增加额外的计算计算负担（CPU/GPU 耗时）。
*   **模拟器依赖**：实验目前仅限于 MuJoCo 模拟环境，尚未在具有复杂传感器噪声和非结构化环境的真实机器人硬件上进行验证。

（完）
