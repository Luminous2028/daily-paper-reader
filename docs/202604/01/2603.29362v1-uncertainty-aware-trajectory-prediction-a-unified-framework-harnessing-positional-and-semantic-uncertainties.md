---
title: "Uncertainty-Aware Trajectory Prediction: A Unified Framework Harnessing Positional and Semantic Uncertainties"
title_zh: 不确定性感知的轨迹预测：一个结合位置与语义不确定性的统一框架
authors: "Jintao Sun, Hu Zhang, Gangyi Ding, Zhedong Zheng"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29362v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 在轨迹预测中联合建模位置和语义不确定性
tldr: 针对自动驾驶中实时地图存在的位置偏差和语义错误，本文提出了一种不确定性感知轨迹预测框架。该框架采用双头架构，通过端到端的方式独立估计位置与语义预测的方差，并将其作为不确定性指标融入预测流程。在 nuScenes 数据集上的实验证明，该方法能有效量化地图不确定性，显著提升了现有轨迹预测模型在 minADE 和 MR 等指标上的表现，增强了系统在复杂环境下的鲁棒性。
selection_source: fresh_fetch
motivation: 实时地图中传感器精度限制导致的位置偏差和场景上下文误解产生的语义错误，严重影响了轨迹预测的准确性。
method: 提出一种统一的双头架构框架，通过双路径端到端地估计位置与语义的不确定性（方差），并将其与预测结果进行融合。
result: 在 nuScenes 数据集上，该框架在多种地图估计方法和预测基准下均显著降低了 minADE、minFDE 和误检率。
conclusion: 通过显式建模并整合位置与语义维度的地图不确定性，该框架有效提升了轨迹预测模型在真实驾驶场景中的鲁棒性。
---

## 摘要
轨迹预测旨在根据历史运动数据的时间范围和环境上下文，预测车辆和行人等动态实体的未来运动。该领域的一个核心挑战是实时地图中固有的不确定性，这主要源于两个方面：(1) 由于传感器限制或环境遮挡导致的位置不准确；(2) 源于对场景上下文误解的语义错误。为了应对这些挑战，我们提出了一个新颖的统一框架，该框架对位置和语义不确定性进行联合建模，并将其显式集成到轨迹预测流水线中。我们的方法采用双头架构，以双通路方式独立估计语义和位置预测，并以端到端的方式推导出预测方差作为不确定性指标。随后，这些不确定性与语义和位置预测相融合，以增强轨迹预测的鲁棒性。我们在 nuScenes 真实驾驶数据集上评估了我们的不确定性感知框架，针对四种地图估计方法和两种轨迹预测基准进行了广泛的实验。结果验证了我们的方法：(1) 能够通过位置和语义两个维度有效地量化地图不确定性；(2) 在多个指标上（包括最小平均位移误差 (minADE)、最小最终位移误差 (minFDE) 和漏检率 (MR)）持续提升了现有轨迹预测模型的性能。代码将在 https://github.com/JT-Sun/UATP 提供。

## Abstract
Trajectory prediction seeks to forecast the future motion of dynamic entities, such as vehicles and pedestrians, given a temporal horizon of historical movement data and environmental context. A central challenge in this domain is the inherent uncertainty in real-time maps, arising from two primary sources: (1) positional inaccuracies due to sensor limitations or environmental occlusions, and (2) semantic errors stemming from misinterpretations of scene context. To address these challenges, we propose a novel unified framework that jointly models positional and semantic uncertainties and explicitly integrates them into the trajectory prediction pipeline. Our approach employs a dual-head architecture to independently estimate semantic and positional predictions in a dual-pass manner, deriving prediction variances as uncertainty indicators in an end-to-end fashion. These uncertainties are subsequently fused with the semantic and positional predictions to enhance the robustness of trajectory forecasts. We evaluate our uncertainty-aware framework on the nuScenes real-world driving dataset, conducting extensive experiments across four map estimation methods and two trajectory prediction baselines. Results verify that our method (1) effectively quantifies map uncertainties through both positional and semantic dimensions, and (2) consistently improves the performance of existing trajectory prediction models across multiple metrics, including minimum Average Displacement Error (minADE), minimum Final Displacement Error (minFDE), and Miss Rate (MR). Code will available at https://github.com/JT-Sun/UATP.

---

## 论文详细总结（自动生成）

这是一份关于论文《Uncertainty-Aware Trajectory Prediction: A Unified Framework Harnessing Positional and Semantic Uncertainties》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注自动驾驶中的**轨迹预测（Trajectory Prediction）**任务。现有的在线高精地图（Online HD Map）估计方法虽然取得了进展，但由于传感器精度限制、环境遮挡或天气变化，生成的地图不可避免地存在两种噪声：
*   **位置偏差（Positional Inaccuracies）：** 道路边界或中心线的坐标点不准。
*   **语义错误（Semantic Errors）：** 对场景上下文理解错误（如将人行道误认为车道线）。
这些噪声会累积并传递给下游的轨迹预测模型，导致预测失效。论文的核心动机是：**不试图完全消除噪声，而是通过显式建模“位置”和“语义”的不确定性，并将其作为辅助信息输入预测模型，从而增强系统对地图错误的鲁棒性。**

### 2. 方法论：核心思想与技术细节
论文提出了一个名为 **UATP** 的统一框架，其核心思想是利用**双头架构（Dual-head Architecture）**来量化模型的不确定性：

*   **双头预测机制：** 框架包含一个主头（Main Head）和一个辅助头（Auxiliary Head）。主头提取深层特征（如 ResNet50 的 res5c），辅助头提取稍浅层的特征（如 res4f）。
*   **位置不确定性（Positional Uncertainty）：** 两个头分别回归地图元素的坐标。通过计算两个头预测结果之间的 **KL 散度（Kullback-Leibler Divergence）** 来衡量位置不确定性。差异越大，表示模型对该位置的预测越不确定。
*   **语义不确定性（Semantic Uncertainty）：** 两个头分别输出分类概率。取两者的平均值作为最终分类得分，并计算两者之间的**均方误差（MSE）**作为语义不确定性的补充指标。
*   **数学证明：** 论文通过一阶泰勒展开推导证明了：两个独立预测头之间的差异（Prediction Difference）与模型参数的方差（Parameter Uncertainty）的平方根成正比，从而为该方法提供了理论支撑。
*   **下游集成：** 将地图坐标、语义得分及其对应的两种不确定性指标拼接（Concatenate），输入到下游的轨迹预测模型（如 Transformer 或 GNN 架构）中。

### 3. 实验设计
*   **数据集：** 使用大规模自动驾驶数据集 **nuScenes**（包含1000个复杂驾驶场景）。此外，在 **Argoverse 2** 上进行了可视化验证。
*   **Benchmark 与对比方法：**
    *   **地图估计基准（4种）：** MapTR, MapTRv2, MapTRv2-Centerline, StreamMapNet。
    *   **轨迹预测基准（2种）：** HiVT（基于 Transformer）, DenseTNT（基于 GNN）。
    *   **对比对象：** 原始基准模型以及之前最先进的不确定性处理方法（如 Gu et al. 2024 的方法）。
*   **评估指标：** 最小平均位移误差（minADE）、最小最终位移误差（minFDE）、漏检率（Miss Rate, MR）。

### 4. 资源与算力
*   **硬件环境：** 使用了 **4 张 NVIDIA GeForce RTX A6000 GPU**（每张显存 49GB）。
*   **训练时长：** 
    *   第一阶段（地图估计）：以 MapTRv2 为例，训练约需 **1 天 2 小时**。
    *   第二阶段（轨迹预测）：每轮（Epoch）训练约需 **9 分钟**。
*   **参数量：** 引入不确定性模块后，地图估计模型的参数量从 40.64M 略微增加到 43.08M，推理时间几乎保持不变。

### 5. 实验数量与充分性
*   **实验规模：** 论文进行了 **8 组核心组合实验**（4 种地图模型 × 2 种预测模型），涵盖了当前主流的算法组合。
*   **消融实验：** 分别验证了“仅使用位置不确定性”和“仅使用语义不确定性”的效果，证明了两者具有互补性。
*   **场景覆盖：** 针对雨天、夜间、复杂路口、遮挡严重等极端场景进行了专门的可视化分析和案例研究。
*   **客观性：** 实验不仅展示了性能提升，还主动分析了一个**失败案例（Failure Case）**，体现了科研的客观性。

### 6. 主要结论与发现
*   **性能提升显著：** 在所有组合中，UATP 均提升了预测精度。例如，在 MapTRv2-Centerline + HiVT 组合下，**minADE 提升了 8%，minFDE 提升了 10%，MR 降低了 22%**。
*   **不确定性的价值：** 显式建模语义不确定性对 DenseTNT（GNN 架构）的提升尤为明显；而 HiVT（Transformer 架构）对位置和语义不确定性都非常敏感。
*   **鲁棒性增强：** 在遮挡严重或地图生成质量较差的情况下，不确定性指标能引导预测模型“降低对错误地图信息的信任”，从而生成更合理的轨迹。

### 7. 优点与亮点
*   **通用性强：** 作为一个“即插即用”的框架，它可以无缝集成到多种现有的在线地图和轨迹预测模型中。
*   **理论与实践结合：** 不仅有严谨的数学推导证明不确定性的合理性，还有丰富的真实场景可视化验证。
*   **细粒度建模：** 不同于以往只关注位置不确定性的工作，本文首次强调了语义（分类）不确定性在轨迹预测中的关键作用。

### 8. 不足与局限
*   **依赖地图质量：** 虽然能缓解噪声，但如果底层地图估计模型完全失效（例如在极端恶劣环境下无法生成任何有效元素），该框架的改进空间也会受限。
*   **异常行为处理：** 论文指出，当人类驾驶员的行为本身不符合常规逻辑（如在平直道路上突然无征兆转向）时，模型仍可能预测失败。
*   **计算开销：** 虽然推理时间增加不多，但双头架构在训练阶段需要处理不同层级的特征，增加了训练的复杂度和显存占用。

（完）
