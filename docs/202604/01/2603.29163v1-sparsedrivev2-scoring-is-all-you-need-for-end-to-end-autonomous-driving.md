---
title: "SparseDriveV2: Scoring is All You Need for End-to-End Autonomous Driving"
title_zh: SparseDriveV2：评分即端到端自动驾驶所需的一切
authors: "Wenchao Sun, Xuewu Lin, Keyu Chen, Zixiang Pei, Xiang Li, Yining Shi, Sifa Zheng"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29163v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 通过轨迹评分对驾驶行为的不确定性进行建模
tldr: SparseDriveV2 深入探讨了端到端自动驾驶中轨迹评分机制的潜力。研究发现，通过增加静态轨迹词汇表的密度，规划性能可持续提升。为此，该研究提出了分解式轨迹表示法，将轨迹拆分为几何路径和速度剖面，并结合粗细两级评分策略，在 NAVSIM 和 Bench2Drive 基准测试中取得了领先性能，证明了大规模静态词汇表评分在自动驾驶中的有效性。
selection_source: fresh_fetch
motivation: 探究静态轨迹词汇表在足够密集的情况下是否能达到甚至超越动态生成方案的性能。
method: 提出一种可扩展的分解式词汇表表示法，将轨迹分解为路径和速度，并采用先粗后细的两阶段评分策略。
result: 在 NAVSIM 上达到 92.0 PDMS，在 Bench2Drive 上以轻量级 ResNet-34 骨干网络取得 89.15 的驾驶得分。
conclusion: 证明了通过大规模、高密度的静态词汇表评分是实现高性能端到端自动驾驶规划的有效途径。
---

## 摘要
端到端多模态规划已被广泛用于建模驾驶行为的不确定性，通常通过对候选轨迹进行评分并选择最优轨迹来实现。现有方法通常分为两类：对大规模静态轨迹词表进行评分，或对一小组动态生成的提议进行评分。虽然静态词表通常受限于动作空间的粗粒度离散化，但动态提议提供了更精细的精度，并在现有基准测试中表现出更强的实证性能。然而，目前尚不清楚动态生成是否具有根本的必要性，或者当静态词表足够密集以覆盖动作空间时，是否已经可以达到相当的性能。在这项工作中，我们首先对代表性的基于评分的方法 Hydra-MDP 进行了系统的扩展性研究，结果表明，随着轨迹锚点变得更加密集，性能会持续提升，且在达到计算限制之前未表现出饱和迹象。受此观察启发，我们提出了 SparseDriveV2，通过两项互补的创新来突破基于评分的规划的性能边界：(1) 一种具有分解结构的可扩展词表表示，将轨迹分解为几何路径和速度剖面，从而实现对动作空间的组合覆盖；(2) 一种可扩展的评分策略，先对路径和速度剖面进行粗粒度的分解评分，随后对一小组组合轨迹进行细粒度评分。通过结合这两项技术，SparseDriveV2 在 NAVSIM 上实现了 92.0 的 PDMS 和 90.1 的 EPDMS，并在 Bench2Drive 上使用轻量级 ResNet-34 作为骨干网络，取得了 89.15 的驾驶分数和 70.00% 的成功率。代码和模型已发布于 https://github.com/swc-17/SparseDriveV2。

## Abstract
End-to-end multi-modal planning has been widely adopted to model the uncertainty of driving behavior, typically by scoring candidate trajectories and selecting the optimal one. Existing approaches generally fall into two categories: scoring a large static trajectory vocabulary, or scoring a small set of dynamically generated proposals. While static vocabularies often suffer from coarse discretization of the action space, dynamic proposals provide finer-grained precision and have shown stronger empirical performance on existing benchmarks. However, it remains unclear whether dynamic generation is fundamentally necessary, or whether static vocabularies can already achieve comparable performance when they are sufficiently dense to cover the action space. In this work, we start with a systematic scaling study of Hydra-MDP, a representative scoring-based method, revealing that performance consistently improves as trajectory anchors become denser, without exhibiting saturation before computational constraints are reached. Motivated by this observation, we propose SparseDriveV2 to push the performance boundary of scoring-based planning through two complementary innovations: (1) a scalable vocabulary representation with a factorized structure that decomposes trajectories into geometric paths and velocity profiles, enabling combinatorial coverage of the action space, and (2) a scalable scoring strategy with coarse factorized scoring over paths and velocity profiles followed by fine-grained scoring on a small set of composed trajectories. By combining these two techniques, SparseDriveV2 achieves 92.0 PDMS and 90.1 EPDMS on NAVSIM, with 89.15 Driving Score and 70.00 Success Rate on Bench2Drive with a lightweight ResNet-34 as backbone. Code and model are released at https://github.com/swc-17/SparseDriveV2.

---

## 论文详细总结（自动生成）

### SparseDriveV2: Scoring is All You Need for End-to-End Autonomous Driving 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
端到端自动驾驶中的规划任务通常采用“多模态评分”机制，即从一组候选轨迹中选出最优路径。现有方法分为两类：**静态词表评分**（简单但受限于离散化精度）和**动态提议生成**（如回归或扩散模型，精度高但模型复杂）。
论文的核心动机在于探讨：**动态生成是否真的必要？** 作者通过对 Hydra-MDP 的扩展性研究发现，只要静态轨迹锚点足够密集，规划性能会持续提升且未见饱和。因此，论文旨在通过一种高效的“分解式”表示和评分策略，在不增加模型复杂度的前提下，通过极高密度的静态词表实现超越动态生成方法的性能。

#### 2. 方法论：核心思想与关键技术
SparseDriveV2 提出了“可扩展词表”和“可扩展评分”两大核心设计：
*   **分解式轨迹表示（Scalable Vocabulary Representation）：**
    *   将时空耦合的轨迹 $\tau$ 分解为**几何路径 $p$**（空间形状，固定空间间隔采样）和**速度剖面 $v$**（时间演化，固定时间间隔的标量速度）。
    *   通过组合 $N_p$ 个路径锚点和 $N_v$ 个速度锚点，可以构建规模为 $N_p \times N_v$ 的超大规模轨迹库（比前作密 32 倍）。
*   **两阶段可扩展评分策略（Scalable Scoring Strategy）：**
    *   **粗粒度分解评分：** 独立对路径和速度进行评分。路径评分利用可变形聚合（Deformable Aggregation）采样场景特征，速度评分利用交叉注意力。
    *   **Top-K 筛选与组合：** 从数万个组合中筛选出高质量的路径和速度子集，组合成少量（如 400 个）候选轨迹。
    *   **细粒度轨迹重调（Re-conditioning）：** 对组合后的轨迹特征进行时空联合推理，并结合规则老师（Rule-based Teacher）的蒸馏信号预测最终得分。
*   **损失函数：** 结合了基于距离的软分类损失（Soft Classification）和基于规则的指标监督损失。

#### 3. 实验设计
*   **数据集与基准：**
    *   **NAVSIM (v1 & v2)：** 数据驱动的非反应式基准，核心指标为 PDMS 和 EPDMS（考虑碰撞、舒适度、进度等）。
    *   **Bench2Drive：** 基于 CARLA 的闭环基准，包含 44 个交互场景，核心指标为 Driving Score 和 Success Rate。
*   **对比方法：** 包括基于评分的方法（VADv2, Hydra-MDP, DriveSuprim）、基于回归的方法（ipad）、基于扩散模型的方法（DiffusionDriveV2）以及混合方法（GTRS）。

#### 4. 资源与算力
*   **GPU 型号与数量：** 
    *   NAVSIM 实验：使用 **8 台 NVIDIA L20 GPU** (48GB 显存)。
    *   Bench2Drive 实验：使用 **16 台 NVIDIA L20 GPU**。
*   **训练细节：** 
    *   NAVSIM 训练 10 个 epoch，总 Batch Size 为 128。
    *   Bench2Drive 采用两阶段训练（感知 100 epoch + 联合训练 10 epoch）。
*   **骨干网络：** 主要使用轻量级的 **ResNet-34**，部分实验对比了 V2-99。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在两个主流基准（NAVSIM 和 Bench2Drive）上进行了详尽测试，涵盖了开环和闭环场景。
*   **消融实验：** 针对词表规模（$N_p$ 和 $N_v$ 的不同组合）、评分模块设计（MHA vs. DFA）、轨迹重调机制等进行了多组消融实验。
*   **充分性评价：** 实验设计较为充分，通过系统性的 Scaling Study 验证了其核心假设（密度即性能），并与多种最新 SOTA 方法进行了公平对比（同骨干网络下的对比），结果具有较强的说服力。

#### 6. 主要结论与发现
*   **静态评分潜力巨大：** 证明了只要轨迹词表足够密集（如 26 万个锚点），纯评分机制可以超越复杂的动态生成或扩散模型。
*   **分解表示的有效性：** 轨迹分解为路径和速度不仅降低了存储压力，还允许模型分别学习空间几何和时间控制。
*   **性能突破：** 在 NAVSIM 上达到 92.0 PDMS，在 Bench2Drive 上达到 89.15 驾驶得分，刷新了多项纪录。

#### 7. 优点
*   **简洁高效：** 避免了扩散模型复杂的迭代去噪过程或回归模型的不稳定性，结构清晰。
*   **可扩展性强：** 提出的分解框架允许在计算预算内轻松扩展词表密度。
*   **性能卓越：** 仅用 ResNet-34 骨干网络就击败了许多使用更大 Backbones 的复杂模型。
*   **理论洞察：** 揭示了端到端规划中“离散化误差”是限制静态评分方法的主要瓶颈。

#### 8. 不足与局限
*   **导航依赖：** 失败案例分析显示，模型在某些复杂路口仍会出现导航决策错误，可能受限于全局导航信息的融合方式。
*   **计算开销：** 虽然采用了两阶段筛选，但处理 26 万个组合在推理初期仍存在一定的计算负担。
*   **静态局限：** 尽管词表很密，但本质上仍是有限的集合，在极端罕见或需要极高精度微调的动作空间中，可能仍不如连续空间的回归方法灵活。

（完）
