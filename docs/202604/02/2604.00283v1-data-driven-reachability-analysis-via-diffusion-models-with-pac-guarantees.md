---
title: Data-Driven Reachability Analysis via Diffusion Models with PAC Guarantees
title_zh: 基于扩散模型且具有 PAC 保证的数据驱动可达性分析
authors: "Yanliang Huang, Peng Xie, Wenyuan Wu, Zhuoqi Zeng, Amr Alanwar"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00283v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 用于可达性分析和不确定性量化的扩散模型
tldr: 本研究提出了一种无需显式模型的非线性动力系统可达性分析框架。该方法利用去噪扩散概率模型（DDPM）从轨迹数据中学习随时间演化的状态分布，并将可达集定义为基于重构误差的非一致性得分的次水平集。通过“先学习后测试”程序校准阈值，确保排除可达状态的概率在统计上受到约束（PAC保证）。实验证明，该方法在处理高维系统时优于传统网格或多项式方法，且经验漏检率低于理论界限。
selection_source: fresh_fetch
motivation: 传统的可达性分析方法通常依赖显式物理模型，且在处理高维非线性系统时面临计算爆炸的挑战。
method: 结合扩散模型学习状态分布，并利用“先学习后测试”框架校准重构误差阈值，以提供概率近似正确（PAC）的理论保证。
result: 在杜芬振子、平面四旋翼及高维反应扩散系统上的实验表明，该方法在保持低漏检率的同时，能有效扩展至高维状态空间。
conclusion: 该框架为复杂非线性系统提供了一种具有统计可靠性且具备良好扩展性的数据驱动可达性分析方案。
---

## 摘要
我们提出了一种用于非线性动力系统可达性分析的数据驱动框架，该框架不需要显式模型。去噪扩散概率模型仅通过轨迹数据学习动力系统随时间演化的状态分布。预测的可达集采用源自重构误差的非一致性得分的次水平集形式，其阈值通过“先学习后测试”（Learn Then Test）程序进行校准，从而确保排除可达状态的概率在高概率下是有界的。在受迫 Duffing 振子、平面四旋翼飞行器和高维反应扩散系统这三个非线性系统上的实验证实，经验漏检率保持在概率近似正确（PAC）界限以下，同时能够扩展到经典基于网格和多项式方法无法处理的状态维度。

## Abstract
We present a data-driven framework for reachability analysis of nonlinear dynamical systems that requires no explicit model. A denoising diffusion probabilistic model learns the time-evolving state distribution of a dynamical system from trajectory data alone. The predicted reachable set takes the form of a sublevel set of a nonconformity score derived from the reconstruction error, with the threshold calibrated via the Learn Then Test procedure so that the probability of excluding a reachable state is bounded with high probability. Experiments on three nonlinear systems, a forced Duffing oscillator, a planar quadrotor, and a high-dimensional reaction-diffusion system, confirm that the empirical miss rate remains below the Probably Approximately Correct (PAC) bound while scaling to state dimensions beyond the reach of classical grid-based and polynomial methods.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合扩散模型（Diffusion Models）与“先学习后测试”（Learn Then Test, LTT）框架的数据驱动可达性分析方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在不依赖显式物理模型（Black-box systems）的情况下，为非线性动力系统计算具有统计可靠性（PAC 保证）且能扩展至高维状态空间的可达集（Reachable Set）。
*   **研究背景**：
    *   **传统方法**：如 Hamilton-Jacobi PDE 或多项式区域增长法，依赖精确解析模型，且面临“维度灾难”，难以处理超过 10 维的系统。
    *   **现有数据驱动方法**：要么依赖学习代理模型（误差会累积），要么使用多项式表示（基函数随维度指数增长），缺乏高维下的有限样本覆盖保证。
*   **整体含义**：本文通过生成式模型直接学习状态分布，绕过了模型构建，并利用统计校准技术确保预测的可达集以极高概率包含真实状态。

### 2. 方法论：核心思想与关键技术
该框架分为两个主要阶段：**分布学习**与**统计校准**。
*   **核心思想**：利用扩散模型（DDPM）的重构误差作为“非一致性得分”（Nonconformity Score），得分越低表示状态越可能属于可达集。
*   **关键技术细节**：
    1.  **时间条件扩散模型**：训练一个以物理时间 $k$ 为条件的去噪网络 $\epsilon_\theta(x_\tau, \tau, k)$。它学习在不同时刻 $t_k$ 系统状态的概率分布。
    2.  **非一致性得分设计**：定义得分为多个低噪声扩散步骤下的重构误差加权和。论文证明了该得分在数学上近似于负对数似然（Negative Log-Likelihood），即得分越小，概率密度越高。
    3.  **LTT 校准程序**：使用“先学习后测试”框架，在校准数据集上寻找一个阈值 $q_k$。通过 Hoeffding-Bentkus p-值计算，确保在给定的风险水平 $\alpha$ 和置信度 $\delta$ 下，漏检率（FNR）受到严格控制。
    4.  **PAC 保证**：最终得到的可达集 $\hat{R}(t_k) = \{x : s(x, k) \leq q_k\}$ 满足概率近似正确（PAC）覆盖，即在 $1-\delta$ 的概率下，真实状态不在该集合内的概率小于 $\alpha$。

### 3. 实验设计
*   **实验场景**：
    1.  **受迫 Duffing 振子 ($n=2$)**：经典的非线性混沌系统，用于验证对复杂吸引子结构的捕捉能力。
    2.  **平面四旋翼飞行器 ($n=6$)**：中等维度的动力学系统，关注水平位置和高度的可达性。
    3.  **Gray-Scott 反应扩散系统 ($n=8192$)**：极高维度的偏微分方程（PDE）系统，验证在大规模状态空间下的扩展性。
*   **对比基准（Benchmarks）**：
    *   **Christoffel 函数法**：基于多项式表示的经典数据驱动方法。
    *   **归一化流（Normalizing Flow, NF）**：另一种基于似然的生成模型。
    *   **变分自编码器（VAE）**：用于高维系统的重构误差对比。
*   **评估指标**：交并比（IoU）、精确度（Precision）和经验漏检率（FNR）。

### 4. 资源与算力
*   **硬件设备**：论文明确提到使用了 **NVIDIA H200 GPU** 进行推理测试。
*   **推理效率**：
    *   Duffing 振子（Batch size 10,000）：单次查询耗时 **0.14 ms**。
    *   Gray-Scott 系统（Batch size 128）：单次查询耗时 **122 ms**。
*   **训练细节**：使用了 AdamW 优化器，学习率在 $10^{-4}$ 到 $5 \times 10^{-4}$ 之间，训练轮数（Epochs）为 100 到 500 不等。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每个系统都进行了完整的训练、校准和测试流程。
    *   **消融实验**：深入探讨了训练轨迹数量 $N$（从 $10^1$ 到 $10^6$）和模型容量（网络深度与宽度）对 IoU 的影响。
    *   **可靠性验证**：对校准过程进行了 **500 次独立随机试验**，以验证 PAC 保证的通过率（均超过了理论底线 $1-\delta$）。
*   **充分性评价**：实验设计非常充分，涵盖了从低维到极高维的跨度，且对比了多种主流生成模型和传统数学方法，客观地展示了扩散模型在处理复杂边界和高维数据上的优势。

### 6. 主要结论与发现
*   **性能优越**：在所有测试系统中，基于扩散模型的得分函数比 NF、VAE 和 Christoffel 方法产生更紧凑（IoU 更高）的可达集。
*   **高维扩展性**：成功处理了 8192 维的 PDE 系统，而传统多项式方法在超过 10 维时就已失效。
*   **理论一致性**：实验观察到的漏检率始终低于预设的 $\alpha$ 水平，验证了 LTT 校准的有效性。
*   **几何保证**：在初始密度有界且系统具有耗散性的假设下，该方法能提供关于漏检区域几何体积的理论上界。

### 7. 优点
*   **无需模型**：完全基于轨迹数据，适用于黑盒系统。
*   **强健的理论保证**：通过 PAC 框架提供了严谨的统计安全边界，这在安全关键型应用中至关重要。
*   **精细的边界捕捉**：相比于多项式方法，扩散模型能更好地刻画非连通区域、薄丝状结构和内部空洞。
*   **计算效率平衡**：通过限制扩散步骤（仅使用低噪声步骤），在保持高精度的同时显著降低了推理开销。

### 8. 不足与局限
*   **数据依赖性**：作为纯数据驱动方法，其性能高度依赖于训练轨迹的覆盖范围。如果初始条件或输入分布采样不足，预测结果可能失效。
*   **推理开销**：虽然进行了优化，但扩散模型仍需多次前向传播（本文为 24 次），相比于单次前向传播的 VAE 或 NF，计算成本仍然较高。
*   **保守性**：为了满足 PAC 保证，校准后的阈值可能会导致预测集合略显保守（即过估计），尤其是在样本量较少时。
*   **静态假设**：假设系统分布是平稳的且校准数据与测试数据独立同分布（i.i.d.），难以直接处理分布偏移（Distribution Shift）或时变系统参数。

（完）
