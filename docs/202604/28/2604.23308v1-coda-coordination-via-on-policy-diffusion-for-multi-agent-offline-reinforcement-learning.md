---
title: "CODA: Coordination via On-Policy Diffusion for Multi-Agent Offline Reinforcement Learning"
title_zh: CODA：通过同策略扩散实现多智能体离线强化学习的协调
authors: "Marcel Hedman, Kale-ab Abebe Tessera, Juan Claude Formanek, Anya Sims, Riccardo Zamboni, Trevor McInroe, John Torr, Elliot Fosong"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23308v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 基于扩散模型的多智能体轨迹生成器
tldr: 离线多智能体强化学习（MARL）常因静态数据集导致协调失败，智能体无法随策略变化进行协同演化。本文提出CODA，一种基于扩散模型的多智能体轨迹生成器，通过根据当前联合策略采样合成经验，模拟在线学习过程。CODA作为通用的增强模块，能有效解决协调病态问题，并在MaMuJoCo等复杂基准测试中表现优异，是实现离线协调行为的重要进展。
selection_source: fresh_fetch
motivation: 传统的离线多智能体强化学习由于依赖静态数据集，导致智能体在训练过程中无法相互适应，容易陷入协调失败。
method: 提出一种基于扩散模型的轨迹生成器CODA，通过以当前联合策略为条件生成合成经验，实现动态的数据增强。
result: 实验证明CODA不仅解决了连续多项式博弈中的协调难题，还在复杂的MaMuJoCo连续控制任务中取得了显著的性能提升。
conclusion: CODA通过模拟在线学习的协同演化机制，为解决离线多智能体强化学习中的协调问题提供了一种算法无关的有效方案。
---

## 摘要
离线多智能体强化学习（MARL）能够从固定数据集中学习策略，但容易出现协调失败：在静态、离策略数据上训练的智能体往往收敛于次优的联合行为，因为它们无法随着策略的变化而进行相互适应。我们提出了 CODA（通过同策略扩散实现多智能体强化学习协调），这是一种基于扩散的多智能体轨迹生成器，用于数据增强。它根据当前的联合策略进行条件采样，生成反映智能体演化行为的合成经验，从而提供了一种相互适应的机制。我们发现，以往基于扩散的增强方法不足以促进多智能体协调，因为它们生成的增强数据集是静态的，不会随着训练过程中当前联合策略的变化而演进；CODA 通过更紧密地模拟同策略学习解决了这一问题，是离线环境下实现协调行为的重要一步。CODA 与算法无关，可以作为增强模块叠加到无模型和基于模型的离线强化学习流水线中。实验表明，CODA 不仅解决了连续多项式博弈中的典型协调病态问题，还在更复杂的 MaMuJoCo 连续控制基准测试中取得了优异的结果。

## Abstract
Offline multi-agent reinforcement learning (MARL) enables policy learning from fixed datasets, but is prone to coordination failure: agents trained on static, off-policy data converge to suboptimal joint behaviours because they cannot co-adapt as their policies change. We introduce CODA (Coordination via On-Policy Diffusion for Multi-Agent Reinforcement Learning), a diffusion-based multi-agent trajectory generator for data augmentation that samples conditioned on the current joint policy, producing synthetic experience which reflects the evolving behaviours of the agents, thereby providing a mechanism for co-adaptation. We find that previous diffusion-based augmentation approaches are insufficient for fostering multi-agent coordination because they produce static augmented datasets that do not evolve as the current joint policy changes during training; CODA resolves this by more closely simulating on-policy learning and is a meaningful step toward coordinated behaviours in the offline setting. CODA is algorithm-agnostic and can be layered onto both model-free and model-based offline reinforcement learning pipelines as an augmentation module. Empirically, CODA not only resolves canonical coordination pathologies in continuous polynomial games but also delivers strong results on the more complex MaMuJoCo continuous-control benchmarks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CODA**（Coordination via On-Policy Diffusion）的方法，旨在解决离线多智能体强化学习（MARL）中的协调失败问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在离线 MARL 中，智能体只能从固定的数据集中学习。由于数据集是静态的，智能体在训练过程中无法像在线学习那样通过互动产生“协同演化（Co-adaptation）”。
*   **病态现象**：现有的离线 MARL 算法（如 MADDPG、MASAC）通常遵循“数据下最佳响应（BRUD）”范式，即每个智能体针对数据集中的静态队友行为进行优化。这导致即使数据集覆盖范围足够，智能体也可能收敛到互不兼容的次优策略，产生**协调失败**。
*   **研究动机**：如何不通过环境交互，在离线设定下恢复智能体之间的协同演化信号。

### 2. 方法论
*   **核心思想**：利用扩散模型（Diffusion Models）作为轨迹生成器，通过**当前联合策略（Current Joint Policy）**进行条件引导，生成合成的“同策略（On-policy）”经验。
*   **关键技术细节**：
    *   **联合轨迹扩散**：训练一个扩散模型来建模整个联合轨迹（包含状态、观测、动作和奖励），捕捉智能体间的时空相关性。
    *   **同策略引导采样**：在采样阶段，CODA 并不只是还原原始数据分布，而是通过两种方式引入当前策略的引导：
        1.  **分类器引导（Classifier Guidance）**：利用当前策略的对数似然梯度 $\nabla \log \pi_{curr}$ 来扰动扩散过程，使生成的动作更符合当前智能体正在学习的行为。
        2.  **无分类器引导（CFG）**：当策略可以用紧凑向量表示时，直接将其作为扩散模型的输入条件。
    *   **策略倾斜分布**：CODA 实际上是从一个受限的分布中采样，该分布既保持在原始数据的支持集内（保证真实性），又向当前策略倾斜（实现协同演化）。
*   **算法流程**：先在离线数据上预训练扩散模型；在 MARL 训练循环中，定期根据当前最新的联合策略生成合成轨迹，并将其与真实数据混合用于更新智能体。

### 3. 实验设计
*   **实验场景**：
    1.  **连续多项式博弈（Polynomial Games）**：包括乘法博弈（Multiplication Game）和双峰博弈（Twin Peaks），用于直观展示协调失败及 CODA 的修复效果。
    2.  **MaMuJoCo**：复杂的连续控制基准测试（如 2-Agent HalfCheetah, 4-Agent Ant）。
*   **Benchmark 与对比方法**：
    *   **基础算法**：MADDPG + 行为克隆（BC）。
    *   **对比方法**：非增强的离线学习、无条件扩散增强（类似合成经验回放）、Q 值条件扩散增强（类似 EAQ）。
*   **评估指标**：归一化平均回报、策略演化轨迹图、动作对数似然（验证引导有效性）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率优化**：文中提到为了降低计算成本，采用了“定期生成”而非“持续生成”合成轨迹的策略（每 4 个 epoch 生成一次），这借鉴了单智能体策略引导扩散的经验。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 MaMuJoCo 任务中使用了 **16 个随机种子**进行聚合，这在强化学习论文中属于较高标准，结果具有较强的统计说服力。
    *   覆盖了多种数据集质量（Replay, Good, Medium, Poor），验证了算法在不同数据分布下的鲁棒性。
*   **充分性评价**：实验设计较为充分。不仅有高维环境的性能对比，还有低维环境的机理分析（可视化策略如何避开次优陷阱），并对引导缩放因子 $\lambda$ 进行了消融实验。

### 6. 主要结论与发现
*   **解决协调失败**：在多项式博弈中，传统方法和无条件扩散均无法逃离次优解，而 CODA 成功引导智能体收敛至全局最优。
*   **同策略转向能力**：实验证明通过调节引导强度 $\lambda$，可以显著提高合成轨迹在当前策略下的似然概率。
*   **性能提升**：在 MaMuJoCo 的 Replay 和 Good 数据集上，CODA 显著优于基准线；但在 Poor 数据集上提升有限，因为扩散模型受限于原始数据的支持集范围。

### 7. 优点
*   **算法无关性**：CODA 作为一个数据增强模块，可以无缝集成到任何无模型或基于模型的离线 MARL 算法中。
*   **理论支撑**：通过重要性采样和对数似然梯度的推导，为扩散引导提供了数学依据。
*   **解决本质问题**：直接针对离线 MARL 中缺失的“协同演化”反馈回路进行建模，而非简单的分布匹配。

### 8. 不足与局限
*   **对数据支持集的依赖**：如果离线数据集完全没有覆盖到最优区域，CODA 的引导作用会受限，因为它被设计为在数据流形内进行采样。
*   **超参数敏感性**：引导缩放因子 $\lambda$ 的选择对结果影响较大，过大会导致轨迹脱离物理真实性，过小则无法提供足够的协调信号。
*   **计算开销**：尽管是定期生成，但扩散模型的采样过程（通常需要数十步迭代）相比直接从 Buffer 读取数据仍增加了显著的训练耗时。

（完）
