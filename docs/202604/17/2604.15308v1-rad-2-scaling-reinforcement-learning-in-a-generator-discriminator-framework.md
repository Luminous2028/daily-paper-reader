---
title: "RAD-2: Scaling Reinforcement Learning in a Generator-Discriminator Framework"
title_zh: RAD-2：在生成器-判别器框架中扩展强化学习
authors: "Hao Gao, Shaoyu Chen, Yifan Zhu, Yuehao Song, Wenyu Liu, Qian Zhang, Xinggang Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15308v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 9.0
evidence: 基于扩散模型的规划器，通过解耦的生成器-判别器设计对多模态未来不确定性建模
tldr: 针对自动驾驶运动规划中扩散模型存在的不稳定性和缺乏负反馈问题，本文提出RAD-2框架。该框架结合了扩散生成器与强化学习优化的判别器，通过解耦设计提升了优化稳定性。引入TC-GRPO算法解决信用分配问题，并利用OGO优化生成器分布。配合高效的BEV-Warp仿真环境，RAD-2显著降低了碰撞率，提升了复杂城市场景下的驾驶平顺性与安全性。
selection_source: fresh_fetch
motivation: 传统的扩散模型规划器在纯模仿学习下缺乏纠错反馈，且在闭环交互中存在随机不稳定性。
method: 采用生成器-判别器架构，利用RL优化的判别器对扩散生成的候选轨迹进行重排序，并结合TC-GRPO和OGO算法进行策略优化。
result: "在仿真中相比强基准扩散规划器降低了56%的碰撞率，并在真实世界部署中表现出更优的安全性与平顺性。"
conclusion: RAD-2证明了通过生成器-判别器框架结合强化学习，能有效提升自动驾驶在复杂环境下的闭环规划性能。
---

## 摘要
高阶自动驾驶要求运动规划器能够建模多模态未来不确定性，同时在闭环交互中保持鲁棒性。尽管基于扩散模型的规划器在建模复杂轨迹分布方面非常有效，但在纯模仿学习训练下，它们往往面临随机不稳定性以及缺乏纠正性负反馈的问题。为了解决这些问题，我们提出了 RAD-2，这是一个用于闭环规划的统一生成器-判别器框架。具体而言，使用基于扩散模型的生成器产生多样化的候选轨迹，而经过强化学习（RL）优化的判别器则根据长期驾驶质量对这些候选轨迹进行重排序。这种解耦设计避免了将稀疏标量奖励直接应用于整个高维轨迹空间，从而提高了优化稳定性。为了进一步增强强化学习，我们引入了时序一致性群体相对策略优化（Temporally Consistent Group Relative Policy Optimization），利用时序相干性来缓解信用分配问题。此外，我们提出了同策略生成器优化（On-policy Generator Optimization），将闭环反馈转化为结构化的纵向优化信号，并逐步将生成器推向高奖励轨迹流形。为了支持高效的大规模训练，我们引入了 BEV-Warp，这是一个高吞吐量仿真环境，通过空间扭曲直接在鸟瞰图（BEV）特征空间中进行闭环评估。与强大的基于扩散模型的规划器相比，RAD-2 将碰撞率降低了 56%。真实世界部署进一步证明了在复杂城市交通中感知安全性和驾驶平顺性的提升。

## Abstract
High-level autonomous driving requires motion planners capable of modeling multimodal future uncertainties while remaining robust in closed-loop interactions. Although diffusion-based planners are effective at modeling complex trajectory distributions, they often suffer from stochastic instabilities and the lack of corrective negative feedback when trained purely with imitation learning. To address these issues, we propose RAD-2, a unified generator-discriminator framework for closed-loop planning. Specifically, a diffusion-based generator is used to produce diverse trajectory candidates, while an RL-optimized discriminator reranks these candidates according to their long-term driving quality. This decoupled design avoids directly applying sparse scalar rewards to the full high-dimensional trajectory space, thereby improving optimization stability. To further enhance reinforcement learning, we introduce Temporally Consistent Group Relative Policy Optimization, which exploits temporal coherence to alleviate the credit assignment problem. In addition, we propose On-policy Generator Optimization, which converts closed-loop feedback into structured longitudinal optimization signals and progressively shifts the generator toward high-reward trajectory manifolds. To support efficient large-scale training, we introduce BEV-Warp, a high-throughput simulation environment that performs closed-loop evaluation directly in Bird's-Eye View feature space via spatial warping. RAD-2 reduces the collision rate by 56% compared with strong diffusion-based planners. Real-world deployment further demonstrates improved perceived safety and driving smoothness in complex urban traffic.

---

## 论文详细总结（自动生成）

这篇论文提出了 **RAD-2**，一个旨在通过强化学习（RL）提升扩散模型规划器在自动驾驶中闭环性能的统一框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：基于扩散模型（Diffusion-based）的规划器在处理多模态轨迹分布方面表现优异，但纯模仿学习（IL）训练存在随机不稳定性，且缺乏纠正性负反馈。
*   **核心挑战**：
    1.  **维度失配**：强化学习的标量奖励（低维）难以直接优化高维且具有物理约束的轨迹空间，导致训练不稳定。
    2.  **信用分配难题**：自动驾驶中瞬时奖励与动作相关性弱，难以确定是轨迹中的哪一部分导致了最终的成功或失败。
    3.  **仿真效率**：现有的游戏引擎或重建类仿真器要么存在巨大的“仿真-现实”差距，要么计算开销过大，难以支持大规模 RL 训练。

### 2. 核心方法论
RAD-2 采用了**生成器-判别器（Generator-Discriminator）**解耦架构，并配合高效仿真环境：
*   **生成器-判别器框架**：
    *   **生成器 (G)**：基于扩散模型，负责生成多样化的候选轨迹。
    *   **判别器 (D)**：基于 Transformer，通过 RL 优化，负责对候选轨迹进行长期质量评估和重排序。这种设计将高维轨迹优化简化为低维的偏好学习。
*   **关键技术细节**：
    1.  **BEV-Warp 仿真环境**：利用 BEV 特征的空间等变性，通过空间扭曲（Warping）直接在特征空间进行闭环仿真，绕过了昂贵的图像渲染，实现了高吞吐量训练。
    2.  **TC-GRPO（时序一致性群体相对策略优化）**：引入“轨迹复用”机制，在固定时域内保持行为连贯性，缓解信用分配问题，使梯度更新更稳定。
    3.  **OGO（同策略生成器优化）**：将闭环反馈转化为结构化的纵向优化信号（如安全减速、效率加速），通过调整纵向分量来引导生成器分布向高奖励区域偏移。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：使用约 50,000 小时的真实世界驾驶数据。
    *   **RL 训练**：从 50k 个真实片段中筛选出 10k 个安全敏感场景和 10k 个效率敏感场景。
*   **Benchmark 与对比方法**：
    *   **仿真环境**：在 BEV-Warp 和光照真实的 3DGS（3D 高斯泼溅）基准上评估。
    *   **对比基准**：包括回归类（TransFuser, VAD, GenAD）、扩散类（ResAD）以及最新的 SOTA 方法（Senna, RAD）。
    *   **评估指标**：碰撞率（CR）、有责碰撞率（AF-CR）、安全裕度（Safety@1s/2s）及导航效率（EP）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率强调**：文中多次强调 BEV-Warp 具有“高吞吐量”和“轻量级”特性，旨在支持大规模 RL 训练，暗示其对算力的利用率高于传统的基于渲染的仿真器。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的闭环仿真测试（512 个安全场景 + 512 个效率场景）。
    *   在 Senna-2 的 6 类典型开环场景中进行了验证。
    *   **消融实验非常充分**：涵盖了训练流水线（IL vs RL）、执行时域（$H_{reuse}$）、剪辑过滤、判别器初始化、群体大小（Group Size）以及推理时扩展（Inference-time Scaling）等多个维度。
*   **客观性**：通过在不同类型的仿真器（特征级 Warp vs 图像级 3DGS）上取得一致的提升，证明了方法的鲁棒性和公平性。

### 6. 主要结论与发现
*   **性能提升**：相比强基准 ResAD，RAD-2 在安全场景下将碰撞率降低了 **56%**，同时显著提升了导航效率。
*   **协同效应**：联合优化生成器和判别器比单独优化其中之一效果更好，展现了良好的 Scaling 行为。
*   **推理扩展性**：增加推理时的候选轨迹数量（M），判别器能有效利用额外算力找到更高质量的解。
*   **实车验证**：在真实城市道路部署中，RAD-2 表现出更强的预见性减速和更平顺的变道决策。

### 7. 优点与亮点
*   **架构创新**：通过解耦生成与评价，解决了 RL 在连续高维动作空间中难以收敛的顽疾。
*   **仿真创新**：BEV-Warp 提供了一种极低成本的闭环训练方案，对工业界大规模训练具有参考价值。
*   **算法改进**：TC-GRPO 结合物理先验（时序一致性），有效解决了自动驾驶 RL 中的噪声梯度问题。

### 8. 不足与局限
*   **表示局限性**：BEV-Warp 强依赖于具有空间等变性的 BEV 特征，对于直接使用原始像素或非网格化潜空间的架构（如某些端到端大模型）可能不适用。
*   **世界模型缺失**：目前主要通过特征扭曲模拟自我运动，对环境中其他动态智能体的复杂交互反馈（Counterfactual response）模拟可能不如完整的生成式世界模型。
*   **算力细节缺失**：缺乏具体的算力消耗数据，难以评估其在不同硬件条件下的复现成本。

（完）
