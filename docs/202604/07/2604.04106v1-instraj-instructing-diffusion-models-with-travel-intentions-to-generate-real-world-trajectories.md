---
title: "InsTraj: Instructing Diffusion Models with Travel Intentions to Generate Real-world Trajectories"
title_zh: InsTraj：利用出行意图引导扩散模型生成真实世界轨迹
authors: "Yuanshao Zhu, Yuxuan Liang, Xiangyu Zhao, Liang Han, Xinwei Fang, Xuetao Wei, James Jianqiao Yu"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04106v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 利用扩散模型从自然语言生成真实的GPS轨迹
tldr: InsTraj是一个利用自然语言指令引导扩散模型生成真实轨迹的框架。针对现有方法难以理解复杂出行意图和处理约束的问题，该研究首先利用大语言模型将非结构化意图转化为语义蓝图，随后通过多模态轨迹扩散Transformer生成符合指令的高保真轨迹。实验证明，该方法在轨迹的真实性、多样性和语义一致性上均显著优于现有技术，为城市规划和移动性模拟提供了新方案。
selection_source: fresh_fetch
motivation: 现有轨迹生成方法缺乏对复杂出行意图的深层语义理解，且难以在满足复杂约束的同时保持人类行为的真实多样性。
method: 提出InsTraj框架，利用大语言模型解析自然语言意图生成语义蓝图，并结合多模态轨迹扩散Transformer进行受控轨迹生成。
result: 在真实世界数据集上的实验表明，InsTraj在生成轨迹的真实性、多样性以及对输入指令的语义忠实度方面均显著优于现有最先进方法。
conclusion: InsTraj成功实现了通过自然语言指令生成高保真且可控的GPS轨迹，有效弥补了意图理解与轨迹生成之间的表征鸿沟。
---

## 摘要
生成真实且可控的 GPS 轨迹是城市规划、移动性模拟和隐私保护数据共享等应用中的一项基础任务。然而，现有方法面临双重挑战：它们缺乏解释复杂用户出行意图的深度语义理解能力，且在处理复杂约束的同时难以保持人类行为固有的真实多样性。为解决这一问题，我们提出了 InsTraj，这是一个直接根据自然语言描述引导扩散模型生成高保真轨迹的新颖框架。具体而言，InsTraj 首先利用强大的大语言模型来解析以自然语言形式构成的非结构化出行意图，从而创建丰富的语义蓝图，并弥合意图与轨迹之间的表示鸿沟。随后，我们提出了一种多模态轨迹扩散 Transformer，它能够整合语义引导，生成符合细粒度用户意图的高保真且忠实于指令的轨迹。在真实世界数据集上的全面实验表明，InsTraj 在生成真实、多样且在语义上忠实于输入指令的轨迹方面，显著优于现有最先进的方法。

## Abstract
The generation of realistic and controllable GPS trajectories is a fundamental task for applications in urban planning, mobility simulation, and privacy-preserving data sharing. However, existing methods face a two-fold challenge: they lack the deep semantic understanding to interpret complex user travel intent, and struggle to handle complex constraints while maintaining the realistic diversity inherent in human behavior. To resolve this, we introduce InsTraj, a novel framework that instructs diffusion models to generate high-fidelity trajectories directly from natural language descriptions. Specifically, InsTraj first utilizes a powerful large language model to decipher unstructured travel intentions formed in natural language, thereby creating rich semantic blueprints and bridging the representation gap between intentions and trajectories. Subsequently, we proposed a multimodal trajectory diffusion transformer that can integrate semantic guidance to generate high-fidelity and instruction-faithful trajectories that adhere to fine-grained user intent. Comprehensive experiments on real-world datasets demonstrate that InsTraj significantly outperforms state-of-the-art methods in generating trajectories that are realistic, diverse, and semantically faithful to the input instructions.

---

## 论文详细总结（自动生成）

这是一份关于论文《InsTraj: Instructing Diffusion Models with Travel Intentions to Generate Real-world Trajectories》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：生成真实且可控的GPS轨迹对于城市规划和隐私保护至关重要。然而，现有生成模型（如GAN、VAE或基础扩散模型）面临两大挑战：
    1.  **语义-时空表征鸿沟**：模型无法理解人类抽象的出行意图（如“去公园散步后购物”），生成的轨迹虽地理上合理，但缺乏目的性。
    2.  **生成控制困境**：在复杂约束下，模型要么退化为确定性的路径规划算法（失去人类行为的多样性），要么为了保持随机性而偏离目标。
*   **核心问题**：如何构建一个能够理解自然语言指令，并生成既符合物理规律又忠实于语义意图的高保真轨迹生成框架。

### 2. 方法论
InsTraj 框架分为三个核心阶段：
*   **意图蒸馏（Intent Distillation）**：
    *   利用 OpenStreetMap (OSM) 的 POI 数据对城市进行网格化功能剖析（如居住区、商业区）。
    *   通过规则和模板提取原始轨迹的语义特征，并利用 GPT-4 进行改写，生成多样化的自然语言指令语料库。
*   **意图编码（Intent Encoding）**：
    *   **LLM2Vec**：将预训练的仅解码器（Decoder-only）LLM（如 Llama-3）转换为强大的双向文本编码器，通过掩码预测和对比学习增强其对空间语义的理解。
    *   **TrajCLIP**：设计了一个多模态对齐模型，用于定量衡量轨迹与指令之间的语义相似度（TIS评分）。
*   **意图对齐生成（Intent-aligned Generation）**：
    *   **MT-DiT (Multimodal Trajectory Diffusion Transformer)**：弃用传统的 U-Net，采用 Transformer 架构。
    *   **联合注意力机制（Joint Attention）**：将轨迹 Patch Token 与指令 Token 拼接，通过双向注意力实现深度融合。
    *   **adaLN-Zero**：利用自适应层归一化注入扩散步数和属性信息，稳定训练并增强控制力。

### 3. 实验设计
*   **数据集**：使用了两个大规模真实世界数据集：**Taxi-Chengdu**（成都出租车）和 **Taxi-Xi'an**（西安出租车）。
*   **Benchmark（对比方法）**：
    *   传统生成模型：VAE, TrajGAN, DP-TrajGAN。
    *   扩散模型：Diffwave, DiffTraj（SOTA）, ControlTraj（SOTA，带拓扑约束）。
    *   为了公平对比，基准模型也通过 LLM 嵌入进行了增强。
*   **评估指标**：
    *   **时空保真度**：JSD（空间密度、行程分布、轨迹长度）。
    *   **指令忠实度**：FCR（功能一致性率）、TIS（轨迹-指令相似度）。
    *   **效率**：推理速度、显存占用。

### 4. 资源与算力
*   **模型参数**：MT-DiT 主干网络约为 34.5 MB；文本编码器基于 Meta-Llama-3-8B。
*   **软件框架**：基于 PyTorch 实现。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但在效率分析中提到，InsTraj 在生成 1000 条轨迹时，比 DiffTraj 和 ControlTraj 快 2 倍以上，且显存占用更低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个城市数据集上进行了全面的 SOTA 对比。
    *   **消融实验**：验证了 LLM 编码器（对比 BERT）和 MT-DiT 架构（对比普通 DiT）的必要性。
    *   **参数敏感性**：分析了 Patch Size 对性能与速度的权衡。
    *   **迁移学习**：测试了从成都到西安的跨城市迁移能力（5% 数据即可达到良好效果）。
    *   **案例研究**：展示了目的驱动、时间模式发现和细粒度属性控制的可视化结果。
*   **评价**：实验设计非常充分且客观。通过引入 TrajCLIP 解决了轨迹-语义对齐难以量化的痛点，多维度的评估证明了其在真实性与受控性之间的平衡。

### 6. 主要结论与发现
*   **性能领先**：InsTraj 在所有时空保真度指标上均达到 SOTA，且在指令忠实度（FCR）上比最强基准提升了约 7-8%。
*   **深度融合的必要性**：实验证明，简单的语义拼接不足以引导生成，MT-DiT 的联合注意力机制是实现语义对齐的关键。
*   **效率优势**：得益于 Patch 化处理和 Transformer 的并行性，该模型在处理长序列轨迹时比传统卷积扩散模型更高效。
*   **空间推理能力**：模型展现出了初步的空间推理迹象，能够根据“指定区域”等动态约束生成合理路径。

### 7. 优点（亮点）
*   **首创性**：首次系统地将“文本-图像生成”的范式引入到“文本-轨迹生成”领域。
*   **架构创新**：MT-DiT 架构有效解决了多模态信息（非结构化文本与结构化时空点）的深度融合问题。
*   **自动化流水线**：提出了一套自动化的意图蒸馏流程，解决了缺乏“指令-轨迹”对齐数据的难题。
*   **实用性强**：支持从粗粒度（去公园）到极细粒度（指定时间、距离、经过点）的灵活控制。

### 8. 不足与局限
*   **数据偏差**：目前仅在出租车数据集上验证，出租车轨迹受交通法规和营运性质影响较大，可能无法完全代表步行、骑行等其他出行模式。
*   **零样本泛化**：虽然迁移学习表现良好，但在完全未见过的城市进行“零样本”生成仍具挑战，因为不同城市的道路拓扑差异巨大。
*   **环境动态性**：模型尚未显式考虑实时交通状况（如突发事故、临时管制）对意图执行的影响。

（完）
