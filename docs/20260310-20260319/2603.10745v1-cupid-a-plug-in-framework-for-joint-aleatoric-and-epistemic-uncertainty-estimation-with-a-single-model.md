---
title: "CUPID: A Plug-in Framework for Joint Aleatoric and Epistemic Uncertainty Estimation with a Single Model"
title_zh: CUPID：一种用于单模型联合偶然不确定性与认知不确定性估计的插件式框架
authors: "Xinran Xu, Xiuyi Fan"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10745v1"
tags: ["query:diffusion-uncertainty"]
score: 8.0
evidence: 联合偶然和认知不确定性估计
tldr: 针对深度学习在医疗、自动驾驶等高风险领域中不确定性估计的需求，本文提出CUPID框架。该框架作为一个通用插件，无需修改或重新训练预训练模型即可同时估计偶然不确定性（Aleatoric）和认知不确定性（Epistemic）。它通过贝叶斯恒等映射和结构化扰动分析实现，支持在任意层插入，为模型提供了模块化、可解释且与模型无关的不确定性评估方案。
selection_source: fresh_fetch
motivation: 现有不确定性估计方法通常只能处理单一类型的不确定性，且往往需要修改或重新训练基础模型，限制了其在实际系统中的应用。
method: CUPID通过学习贝叶斯恒等映射来建模偶然不确定性，并利用结构化扰动下的内部响应分析来捕捉认知不确定性。
result: 在分类、回归和分布外检测等多种任务中，CUPID均取得了竞争性的性能，并能提供关于不确定性来源的层级化见解。
conclusion: CUPID提供了一种模块化、可解释且模型无关的联合不确定性估计方案，增强了AI系统的透明度和信任度。
---

## 摘要
深度学习中不确定性的准确估计对于在医疗诊断和自主决策等高风险领域部署模型至关重要，在这些领域，过度自信的预测可能导致有害后果。在实践中，理解模型不确定性背后的原因及其代表的不确定性类型，可以支持风险感知决策、增强用户信任并指导额外的数据收集。然而，许多现有方法仅处理单一类型的不确定性，或者需要对基础模型进行修改和重新训练，这使得它们难以在实际系统中采用。我们介绍了 CUPID（综合不确定性插件估计模型），这是一个通用模块，可以在不修改或重新训练基础模型的情况下联合估计偶然不确定性（aleatoric uncertainty）和认知不确定性（epistemic uncertainty）。CUPID 可以灵活地插入预训练网络的任何层。它通过学习到的贝叶斯恒等映射对偶然不确定性进行建模，并通过分析模型对结构化扰动的内部响应来捕获认知不确定性。我们在包括分类、回归和分布外检测在内的一系列任务中评估了 CUPID。结果表明，它在提供关于不确定性来源的逐层见解的同时，始终表现出具有竞争力的性能。通过使不确定性估计模块化、可解释且与模型无关，CUPID 支持更透明和更值得信赖的人工智能。相关代码和数据可在 https://github.com/a-Fomalhaut-a/CUPID 获取。

## Abstract
Accurate estimation of uncertainty in deep learning is critical for deploying models in high-stakes domains such as medical diagnosis and autonomous decision-making, where overconfident predictions can lead to harmful outcomes. In practice, understanding the reason behind a model's uncertainty and the type of uncertainty it represents can support risk-aware decisions, enhance user trust, and guide additional data collection. However, many existing methods only address a single type of uncertainty or require modifications and retraining of the base model, making them difficult to adopt in real-world systems. We introduce CUPID (Comprehensive Uncertainty Plug-in estImation moDel), a general-purpose module that jointly estimates aleatoric and epistemic uncertainty without modifying or retraining the base model. CUPID can be flexibly inserted into any layer of a pretrained network. It models aleatoric uncertainty through a learned Bayesian identity mapping and captures epistemic uncertainty by analyzing the model's internal responses to structured perturbations. We evaluate CUPID across a range of tasks, including classification, regression, and out-of-distribution detection. The results show that it consistently delivers competitive performance while offering layer-wise insights into the origins of uncertainty. By making uncertainty estimation modular, interpretable, and model-agnostic, CUPID supports more transparent and trustworthy AI. Related code and data are available at https://github.com/a-Fomalhaut-a/CUPID.

---

## 论文详细总结（自动生成）

这是一份关于论文《CUPID: A Plug-in Framework for Joint Aleatoric and Epistemic Uncertainty Estimation with a Single Model》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：深度学习模型在医疗、自动驾驶等高风险领域应用时，往往存在“过度自信”的问题。现有的不确定性估计方法通常存在以下局限：
    1.  **类型单一**：大多仅关注偶然不确定性（Aleatoric, 数据噪声引起）或认知不确定性（Epistemic, 模型知识不足引起）中的一种。
    2.  **开销巨大**：如集成模型（Ensembles）或贝叶斯神经网络（BNNs）需要改变架构或从头训练，难以适配已有的预训练模型。
*   **研究动机**：开发一种轻量级、模块化且无需重新训练基础模型的插件，能够同时解耦并估计两种不确定性，从而辅助风险感知决策（如医疗影像中区分“图像质量差”还是“遇到罕见病例”）。

### 2. 方法论：CUPID 框架
CUPID（综合不确定性插件估计模型）由三个核心组件组成：**特征提取器**、**重建分支**和**不确定性分支**。
*   **核心思想**：将 CUPID 插入预训练模型的中间层 $l$，利用该层的特征表示进行双重估计。
*   **偶然不确定性（AU）估计**：
    *   通过**不确定性分支**学习一个贝叶斯恒等映射。
    *   假设输出服从异方差高斯分布，通过最大化对数似然函数来预测输入相关的方差 $\sigma^2$。
*   **认知不确定性（EU）估计**：
    *   通过**重建分支**对中间特征进行结构化扰动。
    *   **关键逻辑**：寻找一个与原始特征差异最大、但能保持预测输出不变的扰动特征。
    *   EU 定义为原始预测与扰动后预测之间的差异（$L_1$ 距离）。如果微小的特征改变导致预测剧烈波动，说明模型在该区域缺乏知识。
*   **联合损失函数**：$L_{CUPID} = L_{epis} + \lambda_2 L_{alea}$，其中包含预测一致性约束和特征差异最大化项。

### 3. 实验设计
*   **实验场景与数据集**：
    1.  **医疗影像分类**：GLV2（青光眼检测）、HAM10000（皮肤病变分类）。
    2.  **分布外（OOD）检测**：以 GLV2 为分布内数据，ACRIMA、PAPILA 和 CIFAR-10 为 OOD 数据。
    3.  **图像超分辨率（回归任务）**：Set5、Set14、BSDS100 以及医疗 MRI 数据集 IXI。
    4.  **表格数据分类**：Covertype 数据集。
*   **Benchmark 与对比方法**：
    *   对比了 **Rate-in**、**MC Dropout**、**PostNet**、**IGRUE**、**DEC**、**BNN** 和 **BayesCap** 等 7-8 种主流方法。
    *   评估指标包括 AUC、AURC、Spearman 相关系数、Pearson 相关系数、AUSE（稀疏化误差曲线下面积）和 UCE（不确定性校准误差）。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA GeForce RTX 4090 GPU**。
*   **软件环境**：Python 3.9, PyTorch 2.0.1。
*   **训练时长**：
    *   分类任务（如 GLV2）：训练约 4776 秒，推理约 10 秒。
    *   回归任务（超分辨率）：训练约 26511 秒，推理约 1 秒。
    *   相比于 BNN 或 PostNet，CUPID 的训练时间显著更短，且推理速度极快。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了分类、回归、OOD 检测三大类任务，使用了 8 个以上的数据集。
*   **消融实验**：非常充分。研究了 CUPID 插入的不同层位置（发现 AU 在深层更准，EU 在中早层更敏感）、损失函数项的作用、联合训练 vs 独立训练的效果、以及超参数 $\lambda$ 的敏感性。
*   **客观性**：每组实验重复 3 次并报告均值和标准差，对比方法均在相同 backbone（如 ResNet18, ESRGAN）下进行，确保了公平性。

### 6. 主要结论与发现
*   **性能领先**：CUPID 在误分类检测和 OOD 检测中均达到了 SOTA 或极具竞争力的水平。
*   **成功解耦**：在 1D 回归玩具实验和实际医疗数据中，CUPID 成功区分了噪声引起的 AU 和数据稀疏引起的 EU。
*   **层级演化**：实验发现 AU 在靠近输出的深层特征中表现更明显；而 EU 虽然在深层积累，但在网络中早期插入插件能更好地观察到不确定性的传播。
*   **协同效应**：联合训练 AU 和 EU 分支比单独训练任何一个分支的效果都要好，说明两者具有互补的正则化作用。

### 7. 优点与亮点
*   **即插即用**：无需修改原始模型权重，无需重新训练基础模型，极易集成到现有生产系统中。
*   **通用性强**：同时适用于分类和回归任务，且对 CNN 和 MLP 等架构均有效。
*   **可解释性**：通过层级分析，提供了关于不确定性在网络中如何产生的内部视角。
*   **效率高**：单次前向传播即可获得多种不确定性估计，优于需要多次采样的 MC Dropout。

### 8. 不足与局限
*   **插件训练需求**：虽然不需要训练基础模型，但 CUPID 插件本身仍需要针对特定任务进行训练。
*   **超参数依赖**：损失函数中的权重 $\lambda_1, \lambda_2$ 需要根据具体任务进行微调以达到最佳解耦效果。
*   **架构覆盖**：实验主要集中在 CNN 和 MLP，对于 Transformer 等新兴架构的适配性虽有潜力但文中未做详细展示。
*   **局部线性假设**：EU 的理论推导基于一阶泰勒展开，在模型极度非线性区域可能存在近似误差。

（完）
