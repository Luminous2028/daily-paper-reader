---
title: "PC-Diffuser: Path-Consistent Capsule CBF Safety Filtering for Diffusion-Based Trajectory Planner"
title_zh: PC-Diffuser：面向扩散轨迹规划器的路径一致性胶囊 CBF 安全滤波
authors: "Eugene Ku, Yiwei Lyu"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10330v1"
tags: ["query:diffusion-uncertainty"]
score: 9.0
evidence: 基于扩散模型的自动驾驶轨迹规划器及安全过滤
tldr: 针对自动驾驶中扩散轨迹规划器缺乏安全保证且在分布外场景易失效的问题，本文提出PC-Diffuser框架。该方法将可认证的路径一致性控制障碍函数（CBF）嵌入扩散模型的去噪循环中，通过胶囊几何建模、运动学约束及路径一致性过滤，在保留扩散模型生成路径几何特征的同时，实现了每一步去噪过程中的实时安全修正，显著提升了规划的安全性和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的扩散轨迹规划器在复杂或罕见场景下难以提供安全认证，且容易发生灾难性失效。
method: 在去噪循环中嵌入基于胶囊距离的控制障碍函数和运动学模型，实现路径一致的安全过滤与实时修正。
result: 该方法能在不扭曲路径几何的前提下消除约束违规，使修正后的规划轨迹始终保持在专家行为分布内。
conclusion: PC-Diffuser为基于扩散的规划器提供了一种可认证的安全增强方案，实现了生成式行为与严苛安全约束的有效融合。
---

## 摘要
复杂交通环境下的自动驾驶要求规划器具备超越手工规则的泛化能力，这促使了从专家演示中学习行为的数据驱动方法的发展。基于扩散的轨迹规划器最近通过迭代去噪全时域规划展现了强大的闭环性能，但它们仍难以进行安全性认证，并且在罕见或分布外场景中可能会发生灾难性失败。为了应对这一挑战，我们提出了 PC-Diffuser，这是一个安全增强框架，它将可认证的、路径一致的控制屏障函数（CBF）结构直接嵌入到扩散规划的去噪循环中。其核心思想是将安全性作为轨迹生成的内在组成部分，而非事后修正：我们在保持扩散模型预期的路径几何形状的同时，强制执行轨迹展开过程中的前向不变性。具体而言，PC-Diffuser (i) 使用胶囊距离屏障函数评估碰撞风险，该函数能更好地反映车辆几何形状并减少不必要的保守性；(ii) 在运动学自行车模型下将去噪后的路径点转换为动力学可行的运动；以及 (iii) 应用路径一致的安全滤波器，在不产生几何失真的情况下消除残余的约束违反，从而使修正后的规划保持接近所学习的分布。通过在每个去噪步骤中注入这些安全一致性修正，并将优化后的轨迹反馈回扩散过程，PC-Diffuser 实现了迭代的、上下文感知的安全保障，而非事后修复。

## Abstract
Autonomous driving in complex traffic requires planners that generalize beyond hand-crafted rules, motivating data-driven approaches that learn behavior from expert demonstrations. Diffusion-based trajectory planners have recently shown strong closed-loop performance by iteratively denoising a full-horizon plan, but they remain difficult to certify and can fail catastrophically in rare or out-of-distribution scenarios. To address this challenge, we present PC-Diffuser, a safety augmentation framework that embeds a certifiable, path-consistent barrier-function structure directly into the denoising loop of diffusion planning. The key idea is to make safety an intrinsic part of trajectory generation rather than a post-hoc fix: we enforce forward invariance along the rollout while preserving the diffusion model's intended path geometry. Specifically, PC-Diffuser (i) evaluates collision risk using a capsule-distance barrier function that better reflects vehicle geometry and reduces unnecessary conservativeness, (ii) converts denoised waypoints into dynamically feasible motion under a kinematic bicycle model, and (iii) applies a path-consistent safety filter that eliminates residual constraint violations without geometric distortion, so the corrected plan remains close to the learned distribution. By injecting these safety-consistent corrections at every denoising step and feeding the refined trajectory back into the diffusion process, PC-Diffuser enables iterative, context-aware safeguarding instead of post-hoc repair...

---

## 论文详细总结（自动生成）

以下是对论文《PC-Diffuser: Path-Consistent Capsule CBF Safety Filtering for Diffusion-Based Trajectory Planner》的结构化总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：基于扩散模型（Diffusion-based）的轨迹规划器虽然在长时域规划和处理复杂交互方面表现优异，但缺乏形式化的安全保证。在面对罕见或分布外（OOD）场景时，这类模型容易产生物理上可行但会导致碰撞的轨迹。
*   **研究动机**：现有的安全增强方法（如分类器引导或事后安全过滤）存在三大缺陷：
    1.  未针对车辆动力学诱导的实际执行轨迹（Rollout）进行认证。
    2.  安全修正往往忽略动力学一致性，导致生成的“安全”路径无法被车辆物理执行。
    3.  修正过程过于生硬，容易扭曲规划器的原始意图（路径几何），导致驾驶质量下降或过于保守。

### 2. 论文提出的方法论
PC-Diffuser 的核心思想是将**可认证的、路径一致的控制障碍函数（CBF）**结构直接嵌入到扩散模型的迭代去噪循环中。
*   **胶囊距离障碍函数（Capsule Distance CBF）**：相比传统的圆周建模，该方法将车辆简化为线段（胶囊体），计算线段间的最短距离。这能更精确地描述车辆几何，减少在狭窄空间（如路口）的保守性。
*   **动力学可行性保证**：通过 LQR 跟踪器将去噪后的路径点转换为符合运动学自行车模型的控制输入（加速度和转向角），确保安全认证是基于物理可执行的轨迹。
*   **路径一致性修正（Path-Consistent）**：在安全过滤时，固定转向角（保持路径几何不变），仅通过调节纵向速度（加速度）来满足 CBF 约束。这样可以最大限度保留扩散模型学习到的专家路径分布。
*   **迭代式去噪注入**：在扩散模型的每一步去噪过程 $t$ 中，提取当前的清洁轨迹估计 $\hat{\tau}_0^{(t)}$，应用 PC-CBF 滤波器进行修正，然后将修正后的轨迹重新注入扩散过程。这种“协同进化”的方式让模型在生成过程中不断适应安全约束。

### 3. 实验设计
*   **数据集与场景**：使用 **nuPlan** 闭环机器学习规划基准测试。
    *   **Val14**：包含 14 种场景类型的标准验证集。
    *   **Test14-hard**：规则规划器难以处理的硬核子集。
    *   **All-collision Challenge Set**：专门从 Val14 中提取的、原始扩散规划器 100% 发生碰撞的 68 个极端挑战场景。
*   **对比方法（Baselines）**：
    *   **Vanilla DiffusionPlanner**：原始扩散规划器。
    *   **Classifier Guidance**：基于梯度的分类器引导方法。
    *   **SafeDiffuser (Robust/Relaxed/Time-varying)**：在扩散时间轴上施加 CBF 约束的方法。
    *   **MPC-CBF**：基于优化的一步式安全层。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及训练时长。由于该方法是在预训练的 DiffusionPlanner 基础上进行的推理侧增强，其核心计算开销在于每步去噪中的二次规划（QP）求解和 LQR 跟踪。

### 5. 实验数量与充分性
*   **实验规模**：使用了 nuPlan 约 1300 小时的专家驾驶数据进行评估，涵盖了数千个闭环仿真场景。
*   **消融实验**：论文设计了针对“迭代保障”、“选择性过滤”和“动力学可行性”的消融实验，验证了每个组件对降低碰撞率和提升驾驶得分的贡献。
*   **充分性评价**：实验设计较为充分且客观。通过在“全碰撞挑战集”上的极端测试，有力证明了该方法在处理长尾分布场景下的鲁棒性。对比实验涵盖了当前主流的几种安全增强范式，具有较强的说服力。

### 6. 主要结论与发现
*   **安全性显著提升**：在原始模型 100% 碰撞的挑战集中，PC-Diffuser 将碰撞率降低至 **10.29%**，远优于其他基线方法（基线碰撞率仍在 74%-89% 之间）。
*   **驾驶质量不降反升**：在标准 Val14 和 Test14-hard 测试中，PC-Diffuser 的综合驾驶得分（Composite Score）均高于原始模型，证明安全约束并未以牺牲驾驶性能为代价。
*   **迭代修正的优势**：实验表明，在去噪循环中多次微调比在最后一步进行一次性修正效果更好，能引导模型向安全且符合分布的方向收敛。

### 7. 优点与亮点
*   **路径一致性设计**：巧妙地通过固定转向角、只调速度的方式解决了安全修正与专家分布保持之间的矛盾。
*   **几何建模精确**：引入胶囊距离 CBF，解决了自动驾驶中常见的因包围圆过大导致的“冻结机器人”或过度保守问题。
*   **架构融合**：将形式化方法（CBF）与生成式模型（Diffusion）深度耦合，而非简单的后处理，提供了更强的理论保证。

### 8. 不足与局限
*   **反应式局限**：该框架目前仍属于反应式（Reactive）规划，虽然考虑了预测轨迹，但难以主动预测和诱导人类驾驶员的反应。
*   **仿真环境偏差**：实验依赖于 nuPlan 的 IDM 反应式仿真，这种模拟器在处理复杂路口交互时可能与现实存在偏差，导致约 10% 的碰撞仍无法避免（如被 IDM 车辆“围困”）。
*   **计算延迟**：虽然 QP 求解很快，但在扩散模型的几十个去噪步骤中反复调用 LQR 和 QP，可能会对实时部署的计算频率提出挑战（文中未详细讨论推理耗时）。

（完）
