---
title: Encoding Predictability and Legibility for Style-Conditioned Diffusion Policy
title_zh: 为风格条件扩散策略编码可预测性与易读性
authors: "Adrien Jacquet Crétides, Mouad Abrini, Hamed Rahimi, Mohamed Chetouani"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16368v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 协作环境下的扩散模型轨迹生成
tldr: 在人机协作中，机器人的动作需兼顾效率与可解释性（清晰度）。本文提出风格条件扩散策略（SCDP），通过冻结预训练扩散模型并引入轻量级场景编码器，根据环境歧义性动态调整动作风格。该方法在目标模糊时生成易于理解的夸张动作，在目标明确时恢复高效路径，有效平衡了协作安全与作业效率，提升了人机交互的信任感。
selection_source: fresh_fetch
motivation: 旨在解决机器人动作在追求人类可理解性（清晰度）与执行效率之间的权衡难题，避免在简单场景下产生冗余动作。
method: 提出SCDP框架，通过轻量级编码器和歧义检测模块，在不重训基础策略的前提下引导扩散模型生成特定风格的轨迹。
result: 在操作和导航任务的实验中，该方法在模糊场景下增强了动作清晰度，同时在低歧义场景下保持了最优的执行效率。
conclusion: SCDP通过模块化设计实现了动作风格的动态调节，为提升人机协作的透明度与效率提供了一种灵活且高效的方案。
---

## 摘要
在人机协作中，平衡效率与透明运动是一个核心挑战，因为高表现力的动作往往会产生不必要的时间和能量消耗。在协作环境中，易读性（legibility）使人类观察者能够更好地理解机器人的动作，从而提高安全性和信任度。然而，这些行为会导致次优且夸张的轨迹，在机器人目标已经很明显的低歧义场景中，这些轨迹是多余的。为了解决这一权衡问题，我们提出了风格条件扩散策略（SCDP），这是一个模块化框架，可根据环境配置将预训练扩散模型的轨迹生成约束为易读性或效率。我们的方法利用训练后流水线，冻结基础策略并训练轻量级场景编码器和条件预测器来调节扩散过程。在推理时，歧义检测模块会激活相应的条件，仅针对歧义目标优先考虑表现力运动，否则恢复到高效路径。我们在操纵和导航任务上评估了 SCDP，结果表明，它在歧义设置中增强了易读性，同时在不需要易读性时保持了最佳效率，且无需重新训练基础策略。

## Abstract
Striking a balance between efficiency and transparent motion is a core challenge in human-robot collaboration, as highly expressive movements often incur unnecessary time and energy costs. In collaborative environments, legibility allows a human observer a better understanding of the robot's actions, increasing safety and trust. However, these behaviors result in sub-optimal and exaggerated trajectories that are redundant in low-ambiguity scenarios where the robot's goal is already obvious. To address this trade-off, we propose Style-Conditioned Diffusion Policy (SCDP), a modular framework that constrains the trajectory generation of a pre-trained diffusion model toward either legibility or efficiency based on the environment's configuration. Our method utilizes a post-training pipeline that freezes the base policy and trains a lightweight scene encoder and conditioning predictor to modulate the diffusion process. At inference time, an ambiguity detection module activates the appropriate conditioning, prioritizing expressive motion only for ambiguous goals and reverting to efficient paths otherwise. We evaluate SCDP on manipulation and navigation tasks, and results show that it enhances legibility in ambiguous settings while preserving optimal efficiency when legibility is unnecessary, all without retraining the base policy.

---

## 论文详细总结（自动生成）

以下是对论文《Encoding Predictability and Legibility for Style-Conditioned Diffusion Policy》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在人机协作（HRI）中，机器人动作的**易读性（Legibility）**至关重要——即通过动作本身让观察者快速推断出机器人的意图（目标）。然而，易读的动作通常是夸张且次优的（路径较长），而**高效/可预测（Predictable）**的动作则追求最短路径。
*   **核心矛盾**：现有的方法要么只追求效率，导致在目标模糊时人类难以理解；要么始终保持高易读性，在目标明确的简单场景下造成不必要的能量和时间浪费。
*   **研究动机**：开发一种能够根据环境**空间歧义性（Spatial Ambiguity）**动态调节动作风格的框架，在需要时表现意图，在不需要时保持高效。

### 2. 方法论：风格条件扩散策略（SCDP）
SCDP 采用模块化设计，在不改变预训练扩散模型基础能力的前提下，通过外部模块引导生成过程。
*   **核心思想**：采用“训练后流水线（Post-training pipeline）”，冻结基础扩散策略的权重，仅训练轻量级的环境感知模块。
*   **关键技术组件**：
    1.  **场景编码器（Scene Encoder）**：一个 MLP 架构，输入目标物与干扰物的相对位置和距离，生成环境上下文向量 $c$。
    2.  **风格预测器（Conditioning Predictors）**：包含“易读性”和“可预测性”两个独立的 MLP。它们根据上下文向量生成 FiLM（特征线性调制）参数（$\gamma$ 和 $\beta$）。
    3.  **FiLM 调制**：将生成的参数作用于扩散模型 U-Net 的瓶颈层，通过 $FiLM(h) = \gamma \odot h + \beta$ 调节生成轨迹的分布。
    4.  **歧义检测模块（Ambiguity Detection）**：引入“歧义椭圆（Ellipse of Ambiguity）”几何模型。如果干扰物落在机器人与目标之间的特定椭圆区域内，则判定为歧义场景，激活易读性预测器；否则激活效率预测器。

### 3. 实验设计
*   **实验场景**：
    *   **Block Reach（方块抓取）**：Franka Emika Panda 机械臂在两个随机放置的方块中选择其一。
    *   **Navigation（导航）**：Turtlebot 机器人在房间内向两个潜在目标之一移动。
*   **Benchmark 与对比方法**：
    *   **Diffusion Policy**：标准的目标条件扩散策略（基准）。
    *   **Legibility Diffuser**：基于分类器自由引导（Classifier-free guidance）的现有先进方法。
    *   **训练数据集**：作为性能参考上限/下限。
*   **评估指标**：分离度得分（Detachment score，衡量偏离干扰物的程度）、轨迹效率（路径长度倒数）以及综合两者的**自适应透明度得分（Adaptive Transparency Score）**。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA RTX A3000 GPU** 进行推理和训练。
*   **模型规模**：
    *   基础 Diffusion Policy：约 70M 参数。
    *   场景编码器：约 26,000 参数（极轻量）。
    *   风格预测器：每个约 1M 参数。
*   **训练时长**：基础模型训练 100 epoch，场景编码器 50 epoch，风格预测器在 20% 的特定风格子集上微调 300 epoch。
*   **推理效率**：平均推理时间为 5 秒，与基础模型持平，额外模块带来的延迟可忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：在仿真环境中针对每种任务进行了 **100 次独立推理**评估。
*   **充分性**：实验覆盖了“空间歧义”和“无歧义”两种典型配置，并进行了**真实世界部署**（Franka 机器人），验证了从仿真到现实（Sim-to-Real）的迁移能力。
*   **客观性**：通过引入连续的 Sigmoid 函数来定义自适应得分，客观地量化了模型在不同距离下的切换表现。但实验主要集中在两个目标的简单场景，多目标复杂场景的讨论较少。

### 6. 主要结论与发现
*   **动态平衡**：SCDP 在歧义场景下的易读性接近专门的 Legibility Diffuser，但在无歧义场景下的效率显著优于后者（提升了约 137% 的效率得分）。
*   **任务成功率**：引入风格控制模块后，机械臂到达目标的成功率仍保持在 **0.98 以上**，证明调制过程未破坏基础策略的底层导航/操作能力。
*   **环境感知**：通过场景编码器，模型能够根据物体间的几何关系自动调整轨迹弯曲度，无需人工调节引导权重。

### 7. 优点
*   **模块化与非侵入性**：无需重新训练庞大的基础策略，通过冻结权重和轻量级插件实现功能扩展，便于集成到现有系统中。
*   **环境自适应**：解决了传统易读性算法“一刀切”的问题，实现了“该夸张时夸张，该高效时高效”。
*   **计算高效**：附加模块极小，适合部署在算力受限的移动机器人平台上。

### 8. 不足与局限
*   **数据依赖性**：SCDP 的表现受限于训练数据中包含的风格。如果数据集中没有足够夸张的动作，模型无法生成超越数据分布的极高易读性轨迹（而基于引导的方法可以）。
*   **歧义检测过于简化**：目前的“歧义椭圆”是一种几何启发式方法，可能无法捕捉复杂动态环境或人类心理层面的所有歧义情况。
*   **缺乏用户研究**：虽然定量指标表现良好，但尚未进行人类参与的感知实验（User Study）来验证这些自适应轨迹是否真的提升了人类的信任感和理解速度。

（完）
