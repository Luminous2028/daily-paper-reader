---
title: "CorrectionPlanner: Self-Correction Planner with Reinforcement Learning in Autonomous Driving"
title_zh: CorrectionPlanner：自动驾驶中结合强化学习的自我修正规划器
authors: "Yihong Guo, Dongqiangzi Ye, Sijia Chen, Anqi Liu, Xianming Liu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15771v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 自动驾驶中的自回归运动规划与碰撞预测
tldr: 针对自动驾驶中学习型规划器缺乏自我纠错能力的问题，本文提出CorrectionPlanner。该规划器采用自回归方式，通过“提议-评估-纠错”循环生成运动标记。当碰撞评论家预测到危险时，系统会将不安全标记作为纠错轨迹，引导生成安全动作。结合模仿学习与强化学习训练，该方法显著提升了规划的安全性与鲁棒性。
selection_source: fresh_fetch
motivation: 现有的学习型规划器在生成不安全动作后缺乏显式的自我纠错机制，导致在复杂场景下安全性难以保障。
method: 提出一种基于运动标记的自回归规划器，利用碰撞评论家识别风险，并通过保留不安全动作作为纠错轨迹来重新生成安全动作。
result: "在Waymax平台上将碰撞率降低了20%以上，并在nuPlan基准测试中达到了最先进的规划性能水平。"
conclusion: 通过引入类似语言模型推理轨迹的自我纠错机制，可以显著增强自动驾驶规划器在闭环评估中的安全表现。
---

## 摘要
自动驾驶需要安全的规划，但大多数基于学习的规划器缺乏显式的自我修正能力：一旦提出了不安全的动作，就没有机制来修正它。因此，我们提出了 CorrectionPlanner，这是一种具有自我修正能力的自回归规划器，它将规划建模为“提议、评估和修正”循环中的运动标记（motion-token）生成。在每个规划步骤中，策略会提议一个动作（即运动标记），并由一个学习到的碰撞评论器（collision critic）预测该动作是否会在短时间内引发碰撞。如果评论器预测到碰撞，我们将历史不安全运动标记序列保留为自我修正轨迹（self-correction trace），并以此为条件生成下一个运动标记，重复此过程直到提议出安全的运动标记或满足安全标准。这种由所有不安全运动标记组成的自我修正轨迹，代表了规划器在运动标记空间中的修正过程，类似于语言模型中的推理轨迹（reasoning trace）。我们先使用模仿学习训练规划器，随后利用预训练世界模型的展开（rollouts）进行基于模型的强化学习，该世界模型能够逼真地模拟智能体的反应行为。闭环评估表明，CorrectionPlanner 在 Waymax 上的碰撞率降低了 20% 以上，并在 nuPlan 上实现了最先进的规划得分。

## Abstract
Autonomous driving requires safe planning, but most learning-based planners lack explicit self-correction ability: once an unsafe action is proposed, there is no mechanism to correct it. Thus, we propose CorrectionPlanner, an autoregressive planner with self-correction that models planning as motion-token generation within a propose, evaluate, and correct loop. At each planning step, the policy proposes an action, namely a motion token, and a learned collision critic predicts whether it will induce a collision within a short horizon. If the critic predicts a collision, we retain the sequence of historical unsafe motion tokens as a self-correction trace, generate the next motion token conditioned on it, and repeat this process until a safe motion token is proposed or the safety criterion is met. This self-correction trace, consisting of all unsafe motion tokens, represents the planner's correction process in motion-token space, analogous to a reasoning trace in language models. We train the planner with imitation learning followed by model-based reinforcement learning using rollouts from a pretrained world model that realistically models agents' reactive behaviors. Closed-loop evaluations show that CorrectionPlanner reduces collision rate by over 20% on Waymax and achieves state-of-the-art planning scores on nuPlan.

---

## 论文详细总结（自动生成）

以下是对论文《CorrectionPlanner: Self-Correction Planner with Reinforcement Learning in Autonomous Driving》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的基于学习的自动驾驶规划器（Planners）大多采用“单次推理”模式，缺乏显式的**自我修正（Self-Correction）**能力。一旦模型生成了一个不安全的动作，系统通常没有内部机制在执行前对其进行评估和调整。
*   **研究背景**：受大语言模型（LLM）中“推理轨迹（Reasoning Trace）”和“自我反思”机制的启发，作者希望在运动规划领域引入类似的逻辑。即在动作执行前，通过一个循环机制不断提议、评估并修正动作，从而提高复杂交互场景下的安全性。

### 2. 方法论
CorrectionPlanner 将规划建模为运动标记（Motion-token）的自回归生成过程，核心包含以下三个关键组件：
*   **“提议-评估-修正”循环**：
    1.  **提议（Propose）**：策略网络（Policy）根据历史信息和地图提议一个运动标记。
    2.  **评估（Evaluate）**：一个预训练的**碰撞评论家（Collision Critic）**预测该动作在未来短时间内（如2.5秒）是否会导致碰撞。
    3.  **修正（Correct）**：若预测有风险，该标记被拒绝并存入**纠错轨迹（Correction Trace）**。策略网络随后以该轨迹为条件，重新生成新的标记，直到安全或达到最大修正次数。
*   **纠错轨迹（Correction Trace）**：这是一种在运动标记空间内的“推理过程”。模型通过自注意力机制编码之前失败的尝试，从而在下一次提议中避开危险区域。
*   **两阶段训练方案**：
    1.  **模仿学习（IL）**：在专家数据上进行下个标记预测训练，并初步引入纠错损失，使模型学会基于错误尝试回归专家轨迹。
    2.  **基于模型的强化学习（MBRL）**：利用预训练的**反应式世界模型（SMART）**进行闭环仿真。通过 REINFORCE 算法和安全感知奖励（碰撞惩罚+进度奖励）进一步优化策略，使其在面对非专家场景时具备更强的避障能力。

### 3. 实验设计
*   **数据集与场景**：
    *   **WOMD (Waymo Open Motion Dataset)**：使用 Waymax 模拟器进行闭环评估，涵盖反应式（Reactive）和非反应式（Non-reactive）智能体。
    *   **nuPlan**：在 Val14、Test-Hard 和 Test-Random 等标准分割集上进行测试。
*   **Benchmark 与对比方法**：
    *   对比了包括 **SMART**（基础世界模型规划器）、**DiffusionPlanner**（基于扩散模型）、**PlanTF**、**PLUTO**、**LatentDrive** 等多种最先进的（SOTA）学习型规划器。
*   **评估指标**：碰撞率（Collision）、越野率（Off-road）、进度（Progression）以及 nuPlan 的综合规划得分（Planning Score）。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **8 台 NVIDIA H800 GPU**。
*   **训练细节**：
    *   模仿学习阶段训练 16 个 Epoch。
    *   强化学习阶段训练 3 个 Epoch（针对约 10 万个困难样本进行优化）。
    *   推理延迟：在 H800 上，最佳配置下的单次轨迹推理延迟约为 0.434s，相比无纠错版本（0.357s）增加较小。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流的大规模自动驾驶数据集（Waymo 和 nuPlan）上进行了详尽测试，涵盖了数万个仿真场景。
*   **消融实验**：作者进行了多项深入的消融研究，包括：
    *   不同训练框架（纯 IL vs. IL+RL vs. 纠错机制）的对比。
    *   纠错机制与传统“拒绝采样（Rejection Sampling）”及“候选者筛选（Candidate Selection）”的对比。
    *   碰撞阈值和最大纠错长度对性能的影响。
    *   **零样本泛化（Zero-shot）**实验：在 nuPlan 上训练并在 Waymo 上直接测试。
*   **评价**：实验设计非常充分且客观，通过闭环仿真验证了模型在动态交互中的真实表现，而非仅仅是开环指标。

### 6. 主要结论与发现
*   **安全性显著提升**：在 Waymax 模拟器中，CorrectionPlanner 将碰撞率降低了 **20% 以上**。
*   **SOTA 性能**：在 nuPlan 榜单上取得了领先的规划得分，尤其是在反应式测试中表现优异。
*   **纠错轨迹的有效性**：实验证明，相比于简单的重新采样，利用“纠错轨迹”作为上下文能让模型更有效地跳出局部危险区域。
*   **安全与效率的权衡**：引入纠错机制后，虽然碰撞率大幅下降，但在某些极端场景下进度（Progression）会略微降低，这是因为模型选择了更保守的减速或避让策略。

### 7. 优点
*   **机制创新**：首次将类似 LLM 的推理轨迹概念引入运动标记空间，实现了显式的、可解释的动作修正。
*   **闭环鲁棒性**：通过强化学习和反应式世界模型的结合，解决了学习型规划器在闭环仿真中容易出现的“分布偏移”问题。
*   **泛化能力**：零样本实验显示该方法在不同城市和数据集之间具有良好的迁移能力。

### 8. 不足与局限
*   **对评论家的依赖**：系统的安全性高度依赖于“碰撞评论家”的准确性。如果评论家漏报，纠错机制将无法触发；如果误报过多，则会导致驾驶过于保守。
*   **进度损失**：在某些复杂路口，频繁的纠错可能导致车辆过度礼让，影响通行效率。
*   **计算开销**：虽然延迟在可接受范围内，但多次自回归生成标记相比单次推理仍增加了计算负担，在算力受限的嵌入式平台上部署可能面临挑战。

（完）
