---
title: Seeking Physics in Diffusion Noise
title_zh: 在扩散噪声中探寻物理规律
authors: "Chujun Tang, Lei Zhong, Fangqiang Ding"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14294v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 扩散模型中去噪轨迹的评分与剪枝
tldr: 本研究探讨了视频扩散模型（如DiT）是否编码了物理真实性的信号。研究发现，预训练DiT的中间层特征在不同噪声水平下能区分物理上合理与不合理的视频。基于此，作者提出了一种推理时策略——渐进式轨迹选择。该方法利用在冻结特征上训练的轻量级物理验证器，在去噪过程中对多个并行轨迹进行评分并及早剪枝。实验证明，该方法在PhyGenBench上显著提升了视频的物理一致性，且推理成本低于传统的Best-of-K采样。
selection_source: fresh_fetch
motivation: 探索预训练视频扩散模型的中间层特征是否包含能够预测视频物理真实性的潜在信号。
method: 提出渐进式轨迹选择策略，利用轻量级物理验证器在去噪过程的中间节点对并行轨迹进行评分并剪枝低分候选者。
result: 在PhyGenBench基准测试中，该方法在显著降低推理成本的同时，实现了优于或等同于Best-of-K采样的物理一致性。
conclusion: 冻结的扩散模型特征中蕴含着可恢复的物理线索，通过推理时引导可以有效提升生成视频的物理合理性。
---

## 摘要
视频扩散模型是否编码了能够预测物理合理性的信号？我们探测了预训练扩散 Transformer (DiT) 的中间去噪表示，并发现物理合理与不合理的视频在不同噪声水平下的中间层特征空间中是部分可分的。这种可分性不能完全归因于视觉质量或生成器身份，这表明在冻结的 DiT 特征中存在可恢复的物理相关线索。利用这一观察结果，我们引入了渐进式轨迹选择（progressive trajectory selection），这是一种推理时策略，它使用在冻结特征上训练的轻量级物理验证器，在若干中间检查点对并行去噪轨迹进行评分，并尽早剪枝低分候选轨迹。在 PhyGenBench 上的广泛实验表明，我们的方法在提高物理一致性的同时降低了推理成本，以显著更少的去噪步骤实现了与 Best-of-K 采样相当的结果。

## Abstract
Do video diffusion models encode signals predictive of physical plausibility? We probe intermediate denoising representations of a pretrained Diffusion Transformer (DiT) and find that physically plausible and implausible videos are partially separable in mid-layer feature space across noise levels. This separability cannot be fully attributed to visual quality or generator identity, suggesting recoverable physics-related cues in frozen DiT features. Leveraging this observation, we introduce progressive trajectory selection, an inference-time strategy that scores parallel denoising trajectories at a few intermediate checkpoints using a lightweight physics verifier trained on frozen features, and prunes low-scoring candidates early. Extensive experiments on PhyGenBench demonstrate that our method improves physical consistency while reducing inference cost, achieving comparable results to Best-of-K sampling with substantially fewer denoising steps.

---

## 论文详细总结（自动生成）

这篇论文《Seeking Physics in Diffusion Noise》探讨了预训练视频扩散模型内部是否潜藏着物理规律的认知，并利用这种认知在推理阶段优化生成质量。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的视频生成模型（如 Sora、CogVideoX）虽然视觉效果惊人，但常违背物理常识（如重力异常、碰撞穿模）。作者试图探究：**冻结的预训练视频扩散模型在去噪的中间阶段，其内部特征是否已经包含了预测“物理合理性”的信号？**
*   **研究背景**：现有的物理增强方案通常需要昂贵的重新训练（微调）或在生成后进行筛选（Best-of-N）。如果模型内部特征自带物理判别能力，我们就可以在生成过程中及早发现并放弃那些“注定失败”的轨迹，从而兼顾质量与效率。

### 2. 方法论：渐进式轨迹选择
*   **核心思想**：在去噪过程中并行采样多个轨迹，并在中间时间步使用轻量级验证器对特征进行评分，保留高分轨迹，剪枝低分轨迹。
*   **关键技术细节**：
    1.  **特征探测（Probing）**：研究发现 DiT（Diffusion Transformer）的**中间层（如第10层）**在**中等噪声水平（如 t=600, 400）**下对物理合理性最具辨别力。
    2.  **物理验证器（Physics Verifier）**：一个参数量极小（<1M）的模块。它提取 DiT 的冻结特征，通过**因果自注意力机制（Causal Self-Attention）**建模帧间时序关系，最后输出一个物理合理性评分。
    3.  **算法流程**：
        *   从 $N$ 个随机种子开始并行去噪。
        *   在预设的检查点（如 $t=600$），验证器对所有轨迹评分。
        *   按比例 $\rho$（如 50%）保留顶尖轨迹，终止其余轨迹。
        *   重复此过程直到只剩一个“优胜者”完成最终去噪。

### 3. 实验设计
*   **数据集**：
    *   **训练**：使用 VideoPhy 数据集（约 4,500 个带有人类物理标注的视频）来训练轻量级验证器。
    *   **评估**：在 **PhyGenBench** 基准上测试，涵盖力学、光学、热学、材料特性 4 大类、27 种物理规律。
*   **对比方法**：
    *   **Baseline**：标准单种子生成。
    *   **Random Selection**：随机剪枝轨迹（排除搜索空间扩大的干扰）。
    *   **Best-of-4**：生成 4 个完整视频后选最好的（计算成本最高）。
*   **评估指标**：VQAScore（单帧对齐）、GPT-4o 物理评分（多帧一致性）、人类/GPT-4o 成对偏好比较。

### 4. 资源与算力
*   **GPU 型号**：主要实验在 **NVIDIA A5000 (24GB)** 上运行；扩展实验使用了 **L40S (48GB)** 和 **H200 (80GB)**。
*   **训练时长**：物理验证器的训练非常高效，在单卡上**不到 10 分钟**即可完成。
*   **推理效率**：相比于 Best-of-4，该方法在 CogVideoX-2B 上减少了约 **37% 的生成时间**（490s vs 778s）。

### 5. 实验数量与充分性
*   **实验规模**：在 PhyGenBench 的 160 个提示词上进行了完整测试。
*   **消融与泛化**：
    *   测试了不同层、不同噪声水平下的探测效果。
    *   验证了方法在不同模型架构（CogVideoX-2B, 5B, Wan 2.1-14B）上的泛化能力。
    *   **客观性**：通过“随机选择”作为基准，证明了性能提升确实源于物理验证器的引导，而非单纯的多次采样。实验设计较为严谨，考虑了视觉质量等混淆变量。

### 6. 主要结论与发现
*   **物理信号存在性**：扩散模型的中间特征确实编码了物理合理性，且这种信号在噪声很大时（$t=600$）就已经显现。
*   **层级特性**：中间层比输入层或输出层包含更多语义/物理信息。
*   **高效性**：渐进式剪枝可以在不损失质量的前提下，达到与全量 Best-of-N 采样相当的物理一致性，同时大幅节省算力。

### 7. 优点与亮点
*   **即插即用**：无需微调庞大的生成模型底座，保持权重冻结。
*   **极低开销**：验证器极轻量，推理时的额外计算开销几乎可以忽略不计（<3%）。
*   **理论启发**：证明了生成模型在学习去噪的过程中，自发地学习到了某种程度的“世界模型”物理规律。

### 8. 不足与局限
*   **源依赖性**：验证器对生成器身份敏感（Source-specific），在 A 模型上训练的验证器直接用到 B 模型上效果会下降，需要针对不同底座单独训练。
*   **信号强度有限**：目前的探测 AUC 约在 0.68 左右，虽然有效但并不完美，对于极其复杂的物理交互（如化学反应、精细破碎）判别力仍不足。
*   **上限受限**：如果底座模型本身在所有采样轨迹中都无法生成正确的物理现象，该方法（作为一种选择策略）也无法凭空创造出正确的物理视频。

（完）
