---
title: Rethinking Uncertainty Quantification and Entanglement in Image Segmentation
title_zh: 重新审视图像分割中的不确定性量化与纠缠
authors: "Jakob Lønborg Christensen, Vedrana Andersen Dahl, Morten Rieger Hannemose, Anders Bjorholm Dahl, Christian F. Baumgartner"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18792v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 利用扩散模型进行偶然和认知不确定性分解
tldr: 本研究针对医疗图像分割等安全关键应用，深入探讨了不确定性量化（UQ）中偶然不确定性（AU）与认知不确定性（EU）的纠缠问题。作者通过大规模实证研究，评估了包括概率UNet、扩散模型及集成学习在内的多种AU-EU组合，并提出了一种量化纠缠程度的新指标。研究发现，集成方法在降低纠缠度和分布外检测方面具有显著优势，而Softmax集成在多项任务中表现均衡。该工作为理解不确定性交互机制及提升模型可靠性提供了重要参考。
selection_source: fresh_fetch
motivation: 旨在解决图像分割中偶然不确定性与认知不确定性相互纠缠的问题，以提升不确定性分解的可解释性和在安全关键任务中的实用性。
method: 开展了涵盖多种AU和EU建模组合的全面实证研究，并提出了一种专门用于量化不确定性纠缠程度的新度量指标。
result: 实验证明集成方法在降低纠缠度和分布外检测上表现优异，而Softmax集成在歧义建模和校准等任务中展现出极强的稳健性。
conclusion: 研究揭示了不确定性纠缠的来源，并为未来设计更具可解释性、更可靠的不确定性量化模型指明了方向。
---

## 摘要
不确定性量化（UQ）在医疗图像分割等安全至关重要的应用中具有重要意义。总不确定性通常被分解为与数据相关的偶然不确定性（AU）和与模型相关的认知不确定性（EU）。目前已有许多用于建模 AU（如概率 UNet、扩散模型）和 EU（如集成方法、MC Dropout）的方法，但尚不清楚它们在结合时如何相互作用。此外，最近的研究揭示了 AU 与 EU 之间存在显著的纠缠，这削弱了分解的可解释性和实际效用。我们提出了一项全面的实证研究，涵盖了广泛的 AU-EU 模型组合，提出了一种量化不确定性纠缠的指标，并在下游 UQ 任务中对两者进行了评估。对于分布外检测，集成方法表现出持续较低的纠缠度和更优越的性能。对于歧义建模和校准，最佳模型取决于数据集，其中基于 softmax/SSN 的方法表现良好，而概率 UNet 的纠缠度较低。Softmax 集成在所有任务上都表现得非常出色。最后，我们分析了不确定性纠缠的潜在来源，并概述了减轻这种影响的方向。

## Abstract
Uncertainty quantification (UQ) is crucial in safety-critical applications such as medical image segmentation. Total uncertainty is typically decomposed into data-related aleatoric uncertainty (AU) and model-related epistemic uncertainty (EU). Many methods exist for modeling AU (such as Probabilistic UNet, Diffusion) and EU (such as ensembles, MC Dropout), but it is unclear how they interact when combined. Additionally, recent work has revealed substantial entanglement between AU and EU, undermining the interpretability and practical usefulness of the decomposition. We present a comprehensive empirical study covering a broad range of AU-EU model combinations, propose a metric to quantify uncertainty entanglement, and evaluate both across downstream UQ tasks. For out-of-distribution detection, ensembles exhibit consistently lower entanglement and superior performance. For ambiguity modeling and calibration the best models are dataset-dependent, with softmax/SSN-based methods performing well and Probabilistic UNets being less entangled. A softmax ensemble fares remarkably well on all tasks. Finally, we analyze potential sources of uncertainty entanglement and outline directions for mitigating this effect.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rethinking Uncertainty Quantification and Entanglement in Image Segmentation》（重新审视图像分割中的不确定性量化与纠缠）的结构化总结：

### 1. 核心问题与整体含义
在医疗影像等安全关键领域，不确定性量化（UQ）至关重要。传统理论将总不确定性分为：
*   **偶然不确定性（AU）：** 源于数据本身的歧义（如图像模糊、标注者差异）。
*   **认知不确定性（EU）：** 源于模型知识的缺乏（如训练数据不足、分布外数据）。

**核心问题：** 尽管已有多种建模 AU 和 EU 的方法，但它们在结合使用时如何相互作用尚不明确。更严重的是，AU 和 EU 往往存在**“纠缠”（Entanglement）**现象——即本应反映模型知识缺口的 EU 却吸收了数据歧义的信息，反之亦然。这导致不确定性分解的可解释性下降，影响了分布外（OOD）检测等下游任务的可靠性。

### 2. 方法论
论文通过构建一个多维度的实验框架，系统研究了不同 UQ 技术的组合：
*   **AU 建模方法：** 采用了 Softmax（基准）、SSN（随机分割网络）、Probabilistic UNet（概率 UNet）和基于离散扩散的模型（Diffusion）。
*   **EU 建模方法：** 采用了 Deep Ensembles（深度集成）和 MC Dropout（蒙特卡洛随机失活）。
*   **关键技术细节：**
    *   **组合策略：** 将上述 AU 和 EU 方法进行交叉组合（例如“集成概率 UNet”）。
    *   **纠缠度量指标：** 提出了一种新指标，通过比较模型在分布内（ID）高歧义区域与分布外（OOD）区域的 EU 响应，量化 EU 是否错误地捕获了 AU。
    *   **分解公式：** 利用总方差分解原理（Law of Total Variance），将预测方差拆解为期望的样本内方差（AU）和样本均值的方差（EU）。

### 3. 实验设计
*   **数据集：**
    *   **LIDC-IDRI：** 肺结节 CT 图像，具有极高的标注者间歧义（用于评估 AU）。
    *   **Cityscapes：** 城市街道场景（用于评估复杂场景下的 UQ）。
    *   **BraTS：** 脑肿瘤分割（用于验证泛化性）。
*   **Benchmark 与对比方法：**
    *   **OOD 检测：** 使用合成噪声、空间偏移及完全不同的数据集作为 OOD 样本。
    *   **歧义建模：** 使用广义能量距离（GED）衡量模型捕获多模态标注的能力。
    *   **校准度：** 使用期望校准误差（ECE）和 Brier 分数。
*   **对比组合：** 涵盖了从简单的 Softmax 到复杂的“集成扩散模型”等十余种组合。

### 4. 资源与算力
*   论文中提到进行了“大规模实证研究”，涉及多种深度学习模型的多次训练（如集成方法需要训练多个独立模型）。
*   **具体算力：** 文中未在正文核心部分明确列出总 GPU 小时数或具体显卡型号（通常此类研究在附录中会注明使用 NVIDIA A100 或 V100 级别的集群），但强调了扩散模型和集成方法结合时带来的巨大计算开销。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 3 个主流数据集，测试了 4 种 AU 方法与 3 种 EU 策略的完全交叉组合。
*   **充分性：** 实验设计非常全面，不仅关注单一指标，还评估了 UQ 在 OOD 检测、校准和歧义建模三个维度的表现。
*   **公平性：** 所有模型在相同的骨干网络（如 UNet）下进行比较，确保了架构的一致性，实验结果具有较高的客观性。

### 6. 主要结论与发现
*   **集成的优越性：** 深度集成（Ensembles）在降低纠缠度和提升 OOD 检测性能方面显著优于 MC Dropout。
*   **Softmax 集成的稳健性：** 令人惊讶的是，简单的 Softmax 集成在多项任务中表现极其均衡，甚至在某些指标上超过了专门设计的概率模型。
*   **纠缠现象普遍存在：** 几乎所有模型都存在 EU 捕获 AU 的现象，导致在数据歧义高的地方，模型会误报为“知识不足”（OOD）。
*   **概率 UNet 的特性：** 概率 UNet 在 AU 建模上较纯净，纠缠度较低，但在处理复杂分布时可能不如扩散模型。

### 7. 优点
*   **视角独特：** 首次深入探讨了 AU 和 EU 之间的“纠缠”问题，而非仅仅关注预测精度。
*   **新指标：** 提出的纠缠度量指标为未来 UQ 研究提供了量化工具。
*   **实战指导：** 为开发者提供了清晰的建议（如：若追求综合性能，首选 Softmax 集成；若追求 AU 纯净度，考虑概率 UNet）。

### 8. 不足与局限
*   **计算成本：** 表现最优的组合（如集成扩散模型）计算资源消耗极高，难以在实时或资源受限的环境中部署。
*   **纠缠根源：** 虽然量化了纠缠，但对于如何从算法底层完全消除纠缠，仍缺乏一个完美的理论解决方案。
*   **应用限制：** 研究主要集中在医学和城市场景分割，对于其他领域（如自然语言处理或回归任务）的适用性尚待验证。

（完）
