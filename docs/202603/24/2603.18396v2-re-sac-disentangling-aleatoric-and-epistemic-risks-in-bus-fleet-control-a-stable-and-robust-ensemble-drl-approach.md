---
title: "RE-SAC: Disentangling aleatoric and epistemic risks in bus fleet control: A stable and robust ensemble DRL approach"
title_zh: RE-SAC：公交车队控制中偶然风险与认知风险的解耦：一种稳定且鲁棒的集成深度强化学习方法
authors: "Yifan Zhang, Liang Zheng"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18396v2"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 在公交车队控制中解耦偶然不确定性和认知不确定性
tldr: 针对公交车队控制中随机交通和乘客需求带来的不确定性，本文提出了一种鲁棒集成软演员-评论家（RE-SAC）框架。该方法通过解耦偶然不确定性（噪声）和认知不确定性（数据不足），解决了标准DRL算法在波动环境下的Q值不稳定和策略崩溃问题。RE-SAC结合了基于IPM的权重正则化和多样化Q集成机制，在提高系统鲁棒性的同时，显著降低了分布外状态下的估值误差。
selection_source: fresh_fetch
motivation: 传统DRL算法在处理公交控制时，因无法区分环境噪声和数据稀缺导致Q值估计不稳，进而引发策略崩溃。
method: 提出RE-SAC框架，利用IPM权重正则化应对偶然风险，并通过多样化Q集成惩罚认知风险，实现两种不确定性的显式解耦。
result: "在双向公交走廊模拟中，RE-SAC的累积回报显著优于SAC，且在稀有状态下的Q值估计误差降低了高达62%。"
conclusion: 通过解耦不同来源的风险，RE-SAC为高波动环境下的公交调度提供了一种更稳定、鲁棒的深度强化学习解决方案。
---

## 摘要
由于随机的交通状况和乘客需求，公交驻车控制具有极大的挑战性。尽管深度强化学习（DRL）展现出应用前景，但标准的 Actor-Critic 算法在多变的环境中面临 Q 值不稳定的问题。这种不稳定性的一个关键来源是两种不同不确定性的混淆：偶然不确定性（不可约噪声）和认知不确定性（数据不足）。将两者视为单一风险会导致在噪声状态下的价值低估，从而引发灾难性的策略崩溃。我们提出了一种鲁棒集成软 Actor-Critic（RE-SAC）框架，以显式解耦这些不确定性。RE-SAC 对 Critic 网络应用基于积分概率度量（IPM）的权重正则化以规避偶然风险，在无需昂贵的内循环扰动的情况下，为鲁棒贝尔曼算子提供平滑的解析下界。为了应对认知风险，多样化的 Q 网络集成对数据覆盖稀疏区域中过度自信的价值估计进行惩罚。这种双重机制防止了集成方差将噪声误认为数据缺口，这是我们在消融实验中发现的一种失效模式。在真实的双向公交走廊模拟实验中，结果表明 RE-SAC 相比于原始 SAC（约 -0.55e6）实现了最高的累积奖励（约 -0.4e6）。马氏稀有度分析证实，在稀有的分布外状态下，RE-SAC 将 Oracle Q 值估计误差降低了高达 62%（MAE 为 1647 对比 4343），证明了其在高交通波动性下的卓越鲁棒性。

## Abstract
Bus holding control is challenging due to stochastic traffic and passenger demand. While deep reinforcement learning (DRL) shows promise, standard actor-critic algorithms suffer from Q-value instability in volatile environments. A key source of this instability is the conflation of two distinct uncertainties: aleatoric uncertainty (irreducible noise) and epistemic uncertainty (data insufficiency). Treating these as a single risk leads to value underestimation in noisy states, causing catastrophic policy collapse. We propose a robust ensemble soft actor-critic (RE-SAC) framework to explicitly disentangle these uncertainties. RE-SAC applies Integral Probability Metric (IPM)-based weight regularization to the critic network to hedge against aleatoric risk, providing a smooth analytical lower bound for the robust Bellman operator without expensive inner-loop perturbations. To address epistemic risk, a diversified Q-ensemble penalizes overconfident value estimates in sparsely covered regions. This dual mechanism prevents the ensemble variance from misidentifying noise as a data gap, a failure mode identified in our ablation study. Experiments in a realistic bidirectional bus corridor simulation demonstrate that RE-SAC achieves the highest cumulative reward (approx. -0.4e6) compared to vanilla SAC (-0.55e6). Mahalanobis rareness analysis confirms that RE-SAC reduces Oracle Q-value estimation error by up to 62% in rare out-of-distribution states (MAE of 1647 vs. 4343), demonstrating superior robustness under high traffic variability.

---

## 论文详细总结（自动生成）

这篇论文针对城市公交车队控制中的不确定性问题，提出了一种名为 **RE-SAC** 的鲁棒集成深度强化学习框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：公交串车（Bus Bunching）是城市交通中的顽疾。虽然深度强化学习（DRL）在动态驻车控制中表现出潜力，但在高波动的现实环境中，标准算法（如 SAC）常出现 **“Q值中毒”** 现象，即由于无法区分环境噪声和数据稀缺，导致 Q 值估计严重低估或崩溃。
*   **研究动机**：现有的 DRL 方法混淆了两种本质不同的不确定性：
    1.  **偶然不确定性（Aleatoric Uncertainty）**：由交通和乘客需求的固有随机性引起，不可消除。
    2.  **认知不确定性（Epistemic Uncertainty）**：由经验回放池中数据覆盖不足引起，随数据增加可消除。
    论文旨在通过解耦这两种风险，提升算法在极端和随机环境下的稳定性和鲁棒性。

### 2. 方法论
*   **核心思想**：将鲁棒马尔可夫决策过程（RMDP）与集成学习结合，通过不同的正则化手段分别应对两类风险。
*   **关键技术细节**：
    *   **偶然风险缓解（IPM 正则化）**：利用积分概率度量（IPM）对 Critic 网络进行权重正则化（$\ell_1$ 范数）。这在数学上等同于约束价值函数的 Lipschitz 常数，从而在不进行昂贵的内循环状态扰动的情况下，为鲁棒贝尔曼算子提供解析下界。
    *   **认知风险缓解（Q-Ensemble）**：维护一个由 $K$ 个独立 Q 网络组成的集成。利用集成方差作为认知不确定性的代理指标，在贝尔曼更新中惩罚高方差（即未探索区域）的动作，实现“面对未知时的悲观主义”。
    *   **结构化稳定性证明**：论文通过 **Lean 4** 形式化验证了多惩罚项贝尔曼算子的收敛性。证明指出，使用“冻结参数”（目标网络和固定的 Critic 权重）不仅是经验技巧，更是保证算子收缩性的必要条件。

### 3. 实验设计
*   **场景设置**：一个高保真的双向公交走廊模拟环境，包含 22 个站点，模拟 13 小时的运营（06:00-19:00）。乘客到达服从泊松分布，路段速度服从高斯分布。
*   **对比基准（Benchmarks）**：
    *   **Vanilla SAC**：标准软演员-评论家算法。
    *   **DSAC**：分布深度强化学习（处理风险敏感性）。
    *   **BAC (BEE Actor-Critic)**：旨在通过利用过去成功经验来避免低估的算法。
    *   **消融实验**：仅包含认知风险（Epistemic-only）和仅包含偶然风险（Aleatoric-only）的变体。
*   **评估指标**：累积奖励、Q 值稳定性、马氏稀有度分析（Mahalanobis Rareness，用于评估 OOD 状态下的表现）。

### 4. 资源与算力
*   **算力说明**：论文提到使用 PyTorch 实现，并采用了向量化 Critic 模块以提高效率。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长（小时数）。但提到训练进行了 500 个 Episode，经验回放池大小为 $10^6$，Batch Size 为 2048。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了 4 种主流基准算法。
    *   进行了详细的消融实验，证明了单一风险处理会导致策略崩溃。
    *   测试了不同集成规模（$K=2, 5, 10, 20, 40$）的影响。
    *   每组实验均基于 10 个独立的随机种子进行评估。
*   **充分性评价**：实验设计较为充分且客观。特别是引入了马氏距离来量化状态的“稀有度”，并对比了 Oracle MAE（预测误差），这在鲁棒强化学习领域是非常严谨的验证手段。

### 6. 主要结论与发现
*   **性能优越**：RE-SAC 达到了最高的累积奖励（约 $-0.4 \times 10^6$），显著优于 SAC（$-0.55 \times 10^6$）。
*   **防止崩溃**：消融实验显示，仅考虑认知风险会导致严重的 Q 值低估和策略崩溃，而 RE-SAC 的双重机制成功避免了这一陷阱。
*   **OOD 鲁棒性**：在稀有的分布外（OOD）状态下，RE-SAC 的 Q 值估计误差比 SAC 降低了 62%，证明了其在极端交通状况下的可靠性。
*   **理论发现**：证明了在多惩罚项 RL 中，惩罚项必须与当前被更新的价值函数解耦（即使用冻结参数），否则算子将失去收缩性。

### 7. 优点与亮点
*   **理论与实践结合**：不仅提出了算法，还利用 Lean 4 进行了严谨的数学证明，增强了结论的可信度。
*   **解耦思想清晰**：明确区分了噪声（偶然）和数据缺口（认知），解决了传统鲁棒 RL 过于保守的问题。
*   **计算高效**：通过权重正则化代替了复杂的对抗样本生成，适合实时公交调度。

### 8. 不足与局限
*   **超参数敏感性**：虽然讨论了 $\lambda_{ale}$ 和 $\lambda_{epi}$ 的选择，但在不同交通网络中，这些超参数可能需要精细调优。
*   **单智能体局限**：尽管使用了类别嵌入处理车队，但本质上仍是单智能体框架，在处理超大规模、多线路耦合的复杂线网时，可能不如多智能体（MARL）灵活。
*   **算力细节缺失**：缺乏具体的硬件运行时间数据，难以评估其在大规模部署时的计算成本。

（完）
