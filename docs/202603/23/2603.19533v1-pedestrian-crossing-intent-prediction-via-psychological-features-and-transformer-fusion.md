---
title: Pedestrian Crossing Intent Prediction via Psychological Features and Transformer Fusion
title_zh: 基于心理特征与 Transformer 融合的行人过街意图预测
authors: "Sima Ashayer, Hoang H. Nguyen, Yu Liang, Mina Sartipi"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19533v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 量化行人意图中的认知不确定性
tldr: 本研究针对自动驾驶中的行人过街意图预测，提出了一种轻量化且具有社会感知能力的架构。该方法融合了注意力、位置、场景和交互四个维度的行为流，利用Transformer进行特征融合，并引入变分瓶颈和马氏距离检测器来量化不确定性。在PSI 1.0和2.0基准测试中，该模型凭借可解释的结构化特征取得了优异性能，适用于资源受限平台的风险感知预测。
selection_source: fresh_fetch
motivation: 自动驾驶车辆在城市环境中行驶时，需要准确预测行人的过街意图以确保安全。
method: 提出一种融合四种行为流的轻量化Transformer架构，并结合变分瓶颈与马氏距离检测器来评估预测的不确定性。
result: 在PSI 1.0数据集上达到0.9的F1分数，并在PSI 2.0数据集上建立了领先的基准性能。
conclusion: 该方法通过结构化特征实现了高效且可解释的意图预测，为资源受限环境下的风险感知提供了可靠方案。
---

## 摘要
行人意图预测对于自动驾驶车辆在城市环境中安全行驶至关重要。我们提出了一种轻量级、具备社会感知能力的行人意图预测架构。该架构利用高速公路编码器（highway encoders）、紧凑型 4-token Transformer 以及全局自注意力池化，融合了四种行为流（注意力、位置、情境和交互）。为了量化不确定性，我们引入了两个互补的预测头：一个通过 KL 散度捕捉认知不确定性（epistemic uncertainty）的变分瓶颈（variational bottleneck），以及一个识别分布偏移（distributional shift）的马氏距离（Mahalanobis distance）检测器。这些组件共同产生了校准后的概率和可操作的风险评分，且不牺牲效率。在 PSI 1.0 基准测试中，我们的模型仅使用结构化、可解释的特征，就实现了 0.9 的 F1 分数、0.94 的 AUC-ROC 和 0.78 的 MCC，优于近期的视觉语言模型。在更具多样性的 PSI 2.0 数据集上（据我们所知，目前尚无先前的研究结果），我们建立了 0.78 F1 和 0.79 AUC-ROC 的强力初始基准。基于马氏距离评分的选择性预测在 80% 的覆盖率下，将测试准确率提高了多达 0.4 个百分点。定性注意力热图进一步展示了模型在存在歧义时如何转移其跨流关注点。所提出的方法与模态无关，易于与视觉语言流水线集成，适用于资源受限平台上的风险感知意图预测。

## Abstract
Pedestrian intention prediction needs to be accurate for autonomous vehicles to navigate safely in urban environments. We present a lightweight, socially informed architecture for pedestrian intention prediction. It fuses four behavioral streams (attention, position, situation, and interaction) using highway encoders, a compact 4-token Transformer, and global self-attention pooling. To quantify uncertainty, we incorporate two complementary heads: a variational bottleneck whose KL divergence captures epistemic uncertainty, and a Mahalanobis distance detector that identifies distributional shift. Together, these components yield calibrated probabilities and actionable risk scores without compromising efficiency. On the PSI 1.0 benchmark, our model outperforms recent vision language models by achieving 0.9 F1, 0.94 AUC-ROC, and 0.78 MCC by using only structured, interpretable features. On the more diverse PSI 2.0 dataset, where, to the best of our knowledge, no prior results exist, we establish a strong initial baseline of 0.78 F1 and 0.79 AUC-ROC. Selective prediction based on Mahalanobis scores increases test accuracy by up to 0.4 percentage points at 80% coverage. Qualitative attention heatmaps further show how the model shifts its cross-stream focus under ambiguity. The proposed approach is modality-agnostic, easy to integrate with vision language pipelines, and suitable for risk-aware intent prediction on resource-constrained platforms.

---

## 论文详细总结（自动生成）

### 论文总结：基于心理特征与 Transformer 融合的行人过街意图预测

#### 1. 核心问题与研究动机
*   **核心问题**：如何在复杂的城市交通环境中，准确且实时地预测行人的“过街意图”（内部决策），而非仅仅是“过街行为”（物理轨迹）。
*   **研究背景**：自动驾驶车辆（AV）需要像人类驾驶员一样理解行人的心理状态（如注视、犹豫、意图）。现有的轨迹预测方法难以处理长期不确定性，而纯视觉模型往往忽略了社交和环境上下文。
*   **动机**：利用 PSI（Pedestrian Situated Intent）数据集中的人类驾驶员解释，构建一个轻量化、可解释且具备风险感知能力的模型，以弥合机器感知与人类判断之间的差距。

#### 2. 方法论
该论文提出了一种社会感知的多流 Transformer 融合架构，核心组件包括：
*   **四种行为流特征提取**：
    *   **心理注意力（Attention）**：注视方向、头部朝向、犹豫程度等。
    *   **位置（Positional）**：与路缘的距离、与车辆的相对位置等。
    *   **情境（Situational）**：交通流密度、车速、语义文本嵌入（使用 MPNet 生成）。
    *   **交互（Interaction）**：上述特征的成对乘积，捕捉组合行为。
*   **关键技术细节**：
    *   **高速公路编码器（Highway Encoders）**：独立处理各流特征，通过门控机制平衡特征转换与保留。
    *   **4-Token Transformer**：将四个流编码为四个语义 Token，通过多层 Transformer 编码器捕捉跨模态依赖。
    *   **不确定性量化（双头设计）**：
        *   **变分瓶颈（KL 散度）**：捕捉认知不确定性，衡量样本偏离先验的程度。
        *   **马氏距离（Mahalanobis Distance）**：在嵌入空间检测分布偏移，识别离群样本。
*   **损失函数**：结合了带 MixUp 和标签平滑的交叉熵损失、KL 正则化、特征多样性惩罚以及温度校准先验。

#### 3. 实验设计
*   **数据集**：
    *   **PSI 1.0**：包含 110 个视频，提供意图标注和自然语言解释。
    *   **PSI 2.0**：包含 204 个视频，是目前更多样化、规模更大的扩展版本。
*   **Benchmark 与对比方法**：
    *   对比了 SF-GRU、PCPA、ARN、TrEP 以及最新的视觉语言模型 **ClipCross**。
    *   在 PSI 2.0 上，由于此前无公开结果，该研究建立了首个基准（Baseline）。
*   **评价指标**：Accuracy、F1-score、AUC-ROC、MCC 以及校准指标（ECE、Brier Score）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **模型规模**：提到模型非常轻量化，参数量约为 **100万至200万（1–2 M）**，且在推理阶段不依赖繁重的视频骨干网络，适合资源受限的平台。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 PSI 1.0 和 2.0 上均进行了完整测试，所有结果均为 **5 个随机种子的平均值**（带标准差）。
    *   针对 PSI 1.0 进行了详细的**消融实验**，分别验证了四个行为流对性能的贡献。
    *   进行了深入的**不确定性分析**，包括温度校准前后的对比、错误检测能力（AUROC/AUPRC）以及选择性预测（Selective Prediction）实验。
*   **充分性评价**：实验设计较为全面，不仅关注准确率，还重点分析了模型在面对歧义样本时的可靠性，实验结果客观且具有统计学意义。

#### 6. 主要结论与发现
*   **性能领先**：在 PSI 1.0 上，模型以纯结构化特征达到了 0.9 的 F1 和 0.94 的 AUC，超越了复杂的视觉语言模型。
*   **特征重要性**：消融实验显示，**情境流（Situational）**对性能影响最大，移除后 F1 从 0.9 降至 0.75。
*   **风险感知有效性**：马氏距离是极佳的错误指示器。通过“选择性预测”策略（即放弃处理风险最高的 20% 样本），模型在 PSI 1.0 上的准确率可从 88.8% 提升至 93.5%。
*   **PSI 2.0 基准**：为 PSI 2.0 建立了 0.78 F1 的强力基准，证明了模型在多样化场景下的泛化能力。

#### 7. 优点
*   **轻量且高效**：参数量极小，无需实时运行深度视觉骨干网络，推理延迟低。
*   **可解释性强**：通过 4-token 注意力热图，可以直观观察模型在决策时关注的是注意力、位置还是环境。
*   **鲁棒性高**：引入双重不确定性检测，能够识别出模型“不知道”的场景，这对安全至上的自动驾驶至关重要。
*   **模态无关**：架构设计灵活，易于集成到现有的视觉语言流水线中。

#### 8. 不足与局限
*   **上游依赖**：模型依赖于预先提取的结构化特征（如骨干网络提取的姿态、位置等），若上游感知模块出错，模型性能将受限。
*   **时间建模较弱**：虽然通过短窗口聚合了特征，但相比于长序列 LSTM 或 3D 卷积，对复杂动态时序的建模可能不够精细。
*   **数据规模限制**：尽管使用了 PSI 2.0，但行人意图数据集整体规模相比于通用视觉数据集仍较小，可能存在特定场景的过拟合风险。

（完）
