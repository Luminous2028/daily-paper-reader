---
title: "Uncertainty Matters: Structured Probabilistic Online Mapping for Motion Prediction in Autonomous Driving"
title_zh: 不确定性至关重要：用于自动驾驶运动预测的结构化概率在线建图
authors: "Pritom Gogoi, Faris Janjoš, Bin Yang, Andreas Look"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20076v1"
tags: ["query:diffusion-conf-uncertainty"]
score: 8.0
evidence: 自动驾驶中的概率在线建图与运动预测
tldr: 本研究针对自动驾驶中在线地图生成的结构不确定性问题，提出了一种结构化概率建模框架。传统方法多为确定性或假设点间独立，忽略了道路几何的空间相关性。本文通过低秩加对角（LRPD）协方差分解，同时捕捉全局空间结构和局部噪声，实现了高效的依赖关系建模。在 nuScenes 数据集上的实验证明，该方法显著提升了地图生成精度，并在运动预测任务中取得了 SOTA 表现，强调了不确定性建模在规划任务中的关键作用。
selection_source: fresh_fetch
motivation: 现有的在线地图生成模型通常忽略结构不确定性，或因使用对角协方差矩阵而无法捕捉道路几何中的空间相关性。
method: 提出一种结构化概率建模方法，通过低秩加对角（LRPD）协方差分解来显式建模地图元素内部的依赖关系。
result: 在 nuScenes 数据集上的实验表明，该方法不仅提升了地图生成质量，还在基于地图的运动预测任务中达到了 SOTA 性能。
conclusion: 显式建模地图的结构化不确定性对于提升自动驾驶中的感知与预测任务至关重要。
---

## 摘要
在线地图生成和轨迹预测是自动驾驶“感知-预测-规划”流水线中的关键组件。虽然现代矢量化建图模型实现了较高的几何精度，但它们通常将地图估计视为确定性任务，忽略了结构不确定性。现有的概率方法通常依赖于对角协方差矩阵，这假设了点之间的独立性，无法捕捉道路几何中固有的强空间相关性。为了解决这一问题，我们提出了一种用于在线地图生成的结构化概率公式。我们的方法通过预测稠密协方差矩阵来显式建模元素内部的依赖关系，该矩阵通过低秩加对角（LRPD）协方差分解进行参数化。这种公式将不确定性表示为捕捉全局空间结构的低秩分量和代表独立局部噪声的对角分量的组合，从而在不产生全协方差矩阵高昂计算成本的情况下捕捉几何相关性。在 nuScenes 数据集上的评估表明，与确定性基准相比，我们的不确定性感知框架在在线地图生成质量方面取得了持续的改进。此外，我们的方法在基于地图的运动预测方面创造了新的最先进性能，突显了不确定性在规划任务中的关键作用。代码发布于 link-available-soon。

## Abstract
Online map generation and trajectory prediction are critical components of the autonomous driving perception-prediction-planning pipeline. While modern vectorized mapping models achieve high geometric accuracy, they typically treat map estimation as a deterministic task, discarding structural uncertainty. Existing probabilistic approaches often rely on diagonal covariance matrices, which assume independence between points and fail to capture the strong spatial correlations inherent in road geometry. To address this, we propose a structured probabilistic formulation for online map generation. Our method explicitly models intra-element dependencies by predicting a dense covariance matrix, parameterized via a Low-Rank plus Diagonal (LRPD) covariance decomposition. This formulation represents uncertainty as a combination of a low-rank component, which captures global spatial structure, and a diagonal component representing independent local noise, thereby capturing geometric correlations without the prohibitive computational cost of full covariance matrices. Evaluations on the nuScenes dataset demonstrate that our uncertainty-aware framework yields consistent improvements in online map generation quality compared to deterministic baselines. Furthermore, our approach establishes new state-of-the-art performance for map-based motion prediction, highlighting the critical role of uncertainty in planning tasks. Code is published under link-available-soon.

---

## 论文详细总结（自动生成）

以下是对论文《Uncertainty Matters: Structured Probabilistic Online Mapping for Motion Prediction in Autonomous Driving》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：自动驾驶正从依赖预构建的高精度（HD）地图转向实时在线地图生成（OMG）。然而，在线建图受传感器噪声、遮挡和感知范围限制，具有固有的不确定性。
*   **核心问题**：现有的矢量化建图模型（如 MapTR）大多是确定性的，忽略了预测置信度。少数概率方法（如使用对角协方差矩阵）假设地图点之间相互独立，无法捕捉道路几何结构中固有的**空间相关性**（例如：车道线的曲率意味着相邻点的偏移通常是高度相关的）。
*   **研究动机**：如果忽略这种相关性，生成的不确定性估计会显得“锯齿状”且空间不连贯，导致下游的轨迹预测和规划模块产生错误判断。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出一种结构化概率公式，通过**低秩加对角（Low-Rank plus Diagonal, LRPD）**协方差分解来显式建模地图元素内部的依赖关系。
*   **关键技术细节**：
    *   **概率建模**：将每个地图元素（折线）建模为多变量高斯分布 $P(m_k|x) = \mathcal{N}(m_k|\mu, \Sigma)$。
    *   **LRPD 分解**：将协方差矩阵 $\Sigma$ 分解为 $\Sigma = D + \kappa LL^T$。其中 $D$ 是对角矩阵（代表独立的局部噪声），$L$ 是低秩因子矩阵（捕捉全局空间结构和点间相关性），$\kappa$ 是缩放因子。
    *   **参数效率**：该方法将参数复杂度从 $O(N^2)$ 降低到 $O(NR)$（$R$ 为秩，远小于点数 $N$），在保证表达能力的同时避免了全协方差矩阵的计算负担和训练不稳定性。
    *   **下游集成**：
        1.  **显式不确定性编码**：将预测的均值、对角方差和低秩矩阵行向量拼接，作为轨迹预测编码器的输入。
        2.  **置信度感知特征调制（FiLM）**：利用特征线性调制技术，根据地图元素的分类置信度动态调整点嵌入特征。
*   **算法流程**：采用两阶段训练。第一阶段（Warmup）仅训练对角分量；第二阶段逐渐增加低秩分量的权重，学习空间相关性。

### 3. 实验设计
*   **数据集**：使用 **nuScenes** 数据集（包含波士顿和新加坡的 1000 个驾驶场景）。
*   **基准模型（Benchmark）**：
    *   **建图模型**：MapTR、MapTRv2、MapTRv2-CL（中心线扩展版）。
    *   **预测模型**：HiVT（增强了不确定性感知编码器）。
*   **对比方法**：
    *   确定性回归基准。
    *   独立同分布（i.i.d.）概率模型（如 Gu et al. 提出的 Laplace 分布方法）。
    *   点对相关性模型（如 Zhang et al. 捕捉单点 x-y 相关性的方法）。
*   **评估指标**：建图任务使用 mAP；轨迹预测任务使用 minADE、minFDE 和 Miss Rate (MR)。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了使用 AdamW 优化器，学习率为 $6 \times 10^{-4}$，采用余弦退火调度。建图模型训练了 45 个 epoch（比标准 24 epoch 有所延长以获得更好性能）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三种不同的建图骨干网络（MapTR, MapTRv2, MapTRv2-CL）上验证了 mAP 的提升。
    *   在轨迹预测任务中对比了 8 种以上的配置（包括不同的不确定性表示和特征融合方式）。
    *   进行了关于 FiLM 调制效果的消融实验。
    *   提供了定性的可视化分析（如预测样本的平滑度对比）。
*   **充分性评价**：实验设计较为充分，通过重新运行基准代码确保了对比的公平性（Baseline 也使用了相同的长训练周期），涵盖了从感知到预测的完整链路。

### 6. 主要结论与发现
*   **建图精度提升**：LRPD 结构化不确定性建模不仅能提供更好的置信度估计，还能反哺几何精度的提升（mAP 最高）。
*   **预测性能 SOTA**：在基于在线地图的运动预测任务中达到了新的最先进水平，尤其是在 MapTRv2-CL 骨干网络上，性能非常接近使用真值（GT）地图的下限。
*   **物理一致性**：相比独立不确定性模型产生的“锯齿状”采样，LRPD 生成的地图样本在几何上更平滑、更符合物理逻辑。

### 7. 优点
*   **结构优雅**：通过 LRPD 分解巧妙解决了全协方差矩阵难以训练和计算量大的问题。
*   **实用性强**：该框架与现有的矢量化建图架构兼容，且能显著提升下游预测任务的鲁棒性。
*   **训练稳定**：通过两阶段课程学习（从对角到低秩）解决了复杂概率分布训练易发散的问题。

### 8. 不足与局限
*   **元素间相关性缺失**：目前仅建模了单个地图元素（如一条线）内部的点间相关性，尚未捕捉不同元素之间（如相邻车道线）的相关性。
*   **算力细节不明**：缺乏具体的推理延迟和训练资源消耗数据，难以评估其在嵌入式端侧部署的实际开销。
*   **场景覆盖**：实验主要集中在 nuScenes 城市道路，对于极端天气或更复杂工况的泛化能力尚待验证。

（完）
