---
title: "Vega: Learning to Drive with Natural Language Instructions"
title_zh: Vega：基于自然语言指令的学习驾驶
authors: "Sicheng Zuo, Yuxuan Li, Wenzhao Zheng, Zheng Zhu, Jie Zhou, Jiwen Lu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25741v2"
tags: ["query:diffusion-conf-uncertainty"]
score: 9.0
evidence: 利用扩散范式生成自动驾驶未来轨迹
tldr: 针对现有自动驾驶模型缺乏遵循多样化指令以实现个性化驾驶能力的问题，本文提出了Vega模型。首先构建了包含10万个场景及对应指令和轨迹的大规模数据集InstructScene。Vega采用自回归范式处理视觉和语言输入，并利用扩散范式进行世界建模和轨迹生成。通过联合注意力机制实现多模态交互，Vega在提升规划性能的同时，展现了卓越的指令遵循能力，为智能化和个性化驾驶提供了新方案。
selection_source: fresh_fetch
motivation: 现有自动驾驶模型多用于场景描述或推理，缺乏遵循多样化用户指令以实现个性化驾驶的灵活性。
method: 提出了Vega模型，结合自回归处理视听指令与扩散范式生成预测轨迹，并在新构建的InstructScene数据集上训练。
result: 实验表明Vega在规划性能上表现优异，并具有极强的指令遵循能力。
conclusion: 该研究为开发更智能、更具个性化的自动驾驶系统奠定了基础。
---

## 摘要
视觉-语言-动作模型重塑了自动驾驶，将语言纳入决策过程。然而，大多数现有流程仅利用语言模态进行场景描述或推理，缺乏遵循多样化用户指令以实现个性化驾驶的灵活性。为解决这一问题，我们首先构建了一个大规模驾驶数据集（InstructScene），包含约 100,000 个场景，并标注了多样化的驾驶指令及其对应的轨迹。随后，我们提出了一种统一的视觉-语言-世界-动作模型 Vega，用于基于指令的生成与规划。我们采用自回归范式处理视觉输入（视觉）和语言指令（语言），并采用扩散范式生成未来预测（世界建模）和轨迹（动作）。我们执行联合注意力机制以实现模态间的交互，并为不同模态使用独立的投影层以增强能力。大量实验表明，我们的方法不仅实现了卓越的规划性能，还展现出强大的指令遵循能力，为更智能、更个性化的驾驶系统铺平了道路。

## Abstract
Vision-language-action models have reshaped autonomous driving to incorporate languages into the decision-making process. However, most existing pipelines only utilize the language modality for scene descriptions or reasoning and lack the flexibility to follow diverse user instructions for personalized driving. To address this, we first construct a large-scale driving dataset (InstructScene) containing around 100,000 scenes annotated with diverse driving instructions with the corresponding trajectories. We then propose a unified Vision-Language-World-Action model, Vega, for instruction-based generation and planning. We employ the autoregressive paradigm to process visual inputs (vision) and language instructions (language) and the diffusion paradigm to generate future predictions (world modeling) and trajectories (action). We perform joint attention to enable interactions between the modalities and use individual projection layers for different modalities for more capabilities. Extensive experiments demonstrate that our method not only achieves superior planning performance but also exhibits strong instruction-following abilities, paving the way for more intelligent and personalized driving systems.

---

## 论文详细总结（自动生成）

以下是对论文《Vega: Learning to Drive with Natural Language Instructions》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）驾驶模型虽然引入了语言模态，但大多仅将其用于场景描述或简单的导航命令（如“左转”）。这些模型通常是在“平均专家策略”上进行模仿学习，缺乏遵循用户多样化、个性化指令（如“超车以赶上绿灯”或“靠边停车”）的灵活性。
*   **核心问题**：如何构建一个能够理解开放式自然语言指令，并据此生成准确驾驶轨迹和预测未来场景演变的统一模型？
*   **整体含义**：论文提出了 **Vega**，这是一个统一的“视觉-语言-世界-动作”模型，旨在将自动驾驶从单纯的“模仿驾驶”提升为“指令驱动驾驶”。

### 2. 方法论
*   **核心思想**：通过将**动作规划**与**世界建模（未来图像生成）**联合训练，利用图像生成的密集像素级监督信号来弥补动作监督的稀疏性，从而让模型学习指令、动作与环境变化之间的因果关系。
*   **关键技术细节**：
    *   **混合架构**：采用集成 Transformer 架构。使用**自回归（AR）**范式处理视觉理解和语言指令输入；使用**扩散（Diffusion）**范式生成未来图像预测和动作轨迹。
    *   **Mixture-of-Transformers (MoT)**：为了处理多模态数据的差异，模型为视觉理解、文本处理、图像生成和动作规划分别设置了独立的 Transformer 参数层，实现了模态间的解耦与高效交互。
    *   **联合注意力机制**：所有模态的 Token 在统一的序列中通过因果注意力（Causal Attention）进行交互，确保模型在生成动作和预测未来时能充分参考历史观测和当前指令。
    *   **InstructScene 数据集**：构建了一个包含 10 万个场景的大规模数据集，利用强大的 VLM（如 Qwen2.5-VL）结合基于规则的方法，自动生成多样化的驾驶指令及对应轨迹。

### 3. 实验设计
*   **数据集**：主要使用 **NAVSIM v1** 和 **NAVSIM v2**。
    *   **NAVSIM v1**：基于 OpenScene 过滤的离线仿真环境。
    *   **NAVSIM v2**：引入了更真实的反应式交通流模拟。
*   **Benchmark**：使用 PDMS（预测驾驶员模型评分）和 EPDMS（扩展评分）作为核心评价指标，涵盖碰撞率（NC）、可行驶区域合规性（DAC）、舒适度等。
*   **对比方法**：对比了 TransFuser、HydraMDP++、DriveSuprim、DiffusionDrive、DriveVLA-W0 以及 AutoVLA 等最先进的端到端驾驶模型。

### 4. 资源与算力
*   **硬件设备**：使用了 **8 台 NVIDIA H20 GPU**。
*   **训练细节**：
    *   训练步数：200,000 步。
    *   学习率：2e-5，包含 2500 步预热。
    *   Batch Size：每张显卡为 1（由于模型参数量大且涉及图像生成任务）。
    *   模型规模：基于 Qwen2.5-7B 构建，隐藏层维度为 3584，深度为 28 层。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流 Benchmark（NAVSIM v1/v2）上进行了性能对比。
    *   **消融实验**：针对“未来帧预测任务”、“图像-动作交织序列长度”、“独立动作模块设计”等关键变量进行了 3 组以上的对比实验。
    *   **定性分析**：展示了多组在相同场景下，给定不同指令（如加速、减速、变道）时模型生成的不同轨迹和预测图像。
*   **充分性评价**：实验设计较为全面，不仅验证了规划性能，还通过对比 VLA Baseline 证明了引入“世界建模”任务的必要性。实验过程考虑了 Best-of-N 等常用推理策略，对比具有客观性。

### 6. 主要结论与发现
*   **性能领先**：Vega 在 NAVSIM v2 上取得了 **89.4 EPDMS** 的优异成绩，超越了现有的 SOTA 方法。
*   **密集监督的价值**：实验证明，预测未来图像（世界建模）能显著提升动作规划的准确性，因为它迫使模型理解环境的动态演化。
*   **指令遵循能力**：Vega 能够根据复杂的自然语言指令（如“避开橙色障碍物”）调整驾驶策略，生成的轨迹与指令高度一致。
*   **架构优势**：MoT 架构比单一的 VLM 或扩散模型在处理多任务时表现更稳健，收敛速度更快。

### 7. 优点
*   **多任务统一**：成功将理解、生成、预测和规划整合进一个 Transformer 框架，减少了信息瓶颈。
*   **个性化潜力**：打破了传统模型只能“模仿专家”的限制，支持用户通过自然语言干预驾驶决策。
*   **数据驱动**：通过自动标注流程构建了 InstructScene，为指令驱动驾驶研究提供了重要资源。

### 8. 不足与局限
*   **计算开销**：由于集成了高分辨率图像生成分支，模型的推理延迟和显存占用可能高于纯规划模型，难以直接部署在低算力嵌入式平台上。
*   **视角局限**：目前实验主要基于前视摄像头，虽然架构支持扩展，但在处理侧方或后方复杂交互时可能受限。
*   **指标偏差**：在 NAVSIM v1 上的表现略逊于部分模型，作者指出这是因为 v1 的评分机制更偏向保守策略，而 Vega 学习到了更多样化的驾驶风格。

（完）
